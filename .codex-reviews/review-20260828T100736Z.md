VERDICT: FAIL

## 1. Findings

### High — Unvalidated `generated_at` still makes Cold Trails timezone-dependent

[viewer/app/graph-data.ts:476](/Users/josh/particle-workers/recallweave/viewer/app/graph-data.ts:476) normalizes graph-level `generated_at` with `safeLabel()` rather than `safeIsoTimestamp()`. [viewer/app/cold-trails.ts:325](/Users/josh/particle-workers/recallweave/viewer/app/cold-trails.ts:325) then passes that unvalidated value to `Date.parse()` as the deterministic reference clock.

This accepts non-UTC and malformed schema values:

```text
"2026-08-28"                 => preserved
"08/28/2026"                 => preserved
" 2026-08-28T00:00:00Z "     => preserved
"not-a-date"                  => preserved
```

A timezone-less timestamp is interpreted differently according to the viewer’s local timezone:

```text
TZ=UTC              Date.parse("2026-08-28T00:00:00") = 1787875200000
TZ=America/New_York Date.parse("2026-08-28T00:00:00") = 1787889600000
```

The same imported graph can therefore use reference clocks four hours apart on different machines. Near the 180-day Dormant boundary this changes eligibility; it also changes candidate age bonuses and potentially ordering. If parsing fails entirely, `referenceNowMs()` silently substitutes the latest node timestamp, changing the meaning of the source field rather than rejecting it.

This violates the documented UTC generation-timestamp contract in [docs/json-output.md:210](/Users/josh/particle-workers/recallweave/docs/json-output.md:210) and Cold Trails’ same-input determinism promise. Because reproducibility is an explicit gate, this remains a blocking High finding.

## 2. Missing adversarial tests

- Pass timezone-less `generated_at` through `normalizeGraph()` and assert rejection/null normalization.
- Reject date-only, locale-formatted, padded, and impossible graph-level generation timestamps.
- Run the same normalized graph under two `TZ` settings at the exact 180-day Dormant boundary and assert identical output.
- Assert that invalid `generated_at` cannot silently select the latest node timestamp as a substitute clock.
- Test exact 14-day Parallel-invention eligibility and one millisecond beyond it.
- Add mutation coverage proving that replacing graph-level `safeIsoTimestamp()` with `safeLabel()` fails.
- Assert saved Markdown’s complete expected structure, not merely the presence of selected strings.

## 3. Positive findings

- The harness-provided suite passed: 506 Python tests with one skip, the ResourceWarning run, `compileall`, and all 48 viewer tests.
- The cycle-28b false Parallel-invention claim is fixed: classification now uses the absolute difference between endpoint timestamps.
- Impossible node calendar dates and padded node timestamps are rejected.
- Cold Trails uses a single reference instant throughout a build when that instant is valid.
- Explain revalidates shared tags against both endpoints and mutual neighbors against authored adjacency before displaying them.
- Candidate citations require bilateral citations whose paths match their respective node paths.
- Candidate trails remain labeled `CANDIDATE - NOT A FACT`.
- Candidate trails are suppressed when normalized privacy state contains no passage text.
- Export-history malformed counters are conflict-marked rather than silently trusted.
- Documentation now correctly states that eight trail types are supported.
- No tracked source files were modified during this review.

## 4. Ranked recommended fixes

1. Normalize `generated_at` with the same strict UTC timestamp validator used for node timestamps.
2. Define explicit behavior for invalid or absent generation time. Prefer refusing timestamp-derived trails over silently substituting another clock.
3. Add cross-timezone boundary and mutation tests for graph-level clock normalization.
4. Add exact Parallel-invention boundary and full saved-Markdown snapshot tests.

## 5. Push safety

The tree is **not safe to push** because the unresolved High reproducibility defect fails the milestone gate.

It is also not clean: `.beads/issues.jsonl` is modified, three review Markdown files are untracked, and `uv.lock` is untracked. The repository’s checkpoint policy independently prohibits pushing dirty integration state.