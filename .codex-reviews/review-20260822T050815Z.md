VERDICT: FAIL

## 1. Findings

### High — Contract schema identity was silently removed from Markdown output

Current rendering drops `document["schema_version"]` entirely. The pre-Cycle-7 renderer at base `da58ccd` emitted:

```text
> Schema: recallweave.contract.v1
```

Current [contract_markdown.py](/Users/josh/particle-workers/recallweave/src/recallweave/contract_markdown.py:99) renders handling fields but never routes the top-level schema version to a fence or any other output position.

This removal was not among the explicitly approved appearance changes. It also conflicts with [task-contracts.md](/Users/josh/particle-workers/recallweave/docs/task-contracts.md:237), which calls Markdown a projection “of the same content.” Schema identity is material contract content, not decorative formatting.

The regenerated test was fitted to the omission: [test_contract_markdown.py](/Users/josh/particle-workers/recallweave/tests/test_contract_markdown.py:82) changed from requiring the schema line to explicitly asserting that it is absent. That proves the regression was pinned, not that it was approved.

Failure scenario: a stored or detached Markdown contract can no longer identify which contract schema it projects, preventing consumers and reviewers from distinguishing future schema versions.

No separate runtime reproduction was run because the harness explicitly prohibited rerunning and the sandbox is read-only. The before/after renderer and test diffs reproduce the behavioral change directly.

## 2. Missing adversarial tests

- A compatibility test requiring the top-level `schema_version` to survive the Markdown restructuring and appear inside a `CodeFence`.
- A projection-completeness test enumerating all previously rendered semantic fields, ensuring approved formatting changes do not silently delete them.
- A golden-output review that compares semantic field coverage against `da58ccd`; the current golden only blesses the new output.
- A test asserting that changing `document["schema_version"]` changes fenced Markdown content while leaving the trusted AST structure unchanged.
- A strict test-extra availability gate that fails rather than skips if `mistletoe` is missing. CI currently installs it, but the authoritative security tests remain skip-based.

## 3. Positive findings

- The recorded harness result is clean: 322 tests passed; `compileall` passed.
- All six prior escaping classes, including bare CR and indented markers in the former handling blockquote, are structurally eliminated by uniform fenced rendering.
- Fence length is strictly greater than the longest backtick run in the content, preventing document text from closing its own fence.
- CRLF and bare CR are normalized before fence construction.
- Rendered document strings and permissively typed numeric scalars currently emitted by the renderer are fenced.
- No document-derived text reaches headings, links, tables, blockquotes, list items, HTML, or info strings.
- The parser-backed matrix covers all current string-rendering positions across headings, lists, blockquotes, tables, links, images, HTML, multiple fence lengths, CR, CRLF, and nested constructs.
- The Cycle-6 falsy-citation behavior remains truthiness-based.
- The JSON schema and document builder were not changed by Cycle 7.
- `git diff --check da58ccd..HEAD` is clean.

## 4. Ranked recommended fixes

1. Restore top-level schema identity in an inert form, preferably a trusted `Contract schema:` label followed by `_fenced_esc(document["schema_version"])`.
2. Replace the assertion requiring schema absence with a compatibility and AST-inertness assertion requiring its fenced presence.
3. Add a semantic projection-coverage test anchored to the pre-Cycle-7 renderer’s fields.
4. Make missing `mistletoe` a hard test failure in the authoritative AST module or add an explicit dependency-presence test before discovery.

## 5. Push safety

The tree is **not safe to push** because it contains an unresolved High backward-compatibility regression. It is also not clean: `.beads/interactions.jsonl` is modified and `.beads/issues.jsonl` is untracked.