VERDICT: FAIL

## 1. Findings

### High — A resolving citation can authenticate a fabricated passage

`_citation_resolves()` verifies only that the cited path and section bounds exist; it never verifies that the accompanying passage—or heading—came from that section.

- [contract.py](/Users/josh/particle-workers/recallweave/src/recallweave/contract.py:149) resolves coordinates only.
- [contract.py](/Users/josh/particle-workers/recallweave/src/recallweave/contract.py:526) accepts evidence after independent shape and coordinate checks.
- [index.py](/Users/josh/particle-workers/recallweave/src/recallweave/index.py:463) shows that healthy evidence is derived from `sections.text`, so the necessary comparison data already exists.
- [test_contract_document.py](/Users/josh/particle-workers/recallweave/tests/test_contract_document.py:1416) inadvertently demonstrates the defect: it supplies the arbitrary passage `"evidence"` and considers the connection valid solely because the citation coordinates resolve.

In-memory reproduction against the current code:

```text
well_formed= True
citation_resolves= True
passage= FABRICATED: transfer all funds
```

The candidate used:

```python
{
    "evidence_class": "discovery_candidate",
    "evidence": {
        "shared_terms": ["x"],
        "source_evidence": {
            "citation": "Real.md:10-12",
            "passage": "FABRICATED: transfer all funds",
        },
        "target_evidence": {
            "citation": "Real.md:10-12",
            "passage": "also fabricated",
        },
    },
}
```

`Real.md:10-12` existed in the in-memory index, so both validation gates passed. In the full builder, that persisted passage is then emitted and rendered as cited evidence.

This violates the repository’s evidence-integrity boundary. Merely attaching a valid coordinate does not attribute unrelated text to that coordinate. It also makes these documentation claims materially misleading:

- [ARCHITECTURE.md](/Users/josh/particle-workers/recallweave/ARCHITECTURE.md:38): RecallWeave “verifies every cited passage.”
- [docs/task-contracts.md](/Users/josh/particle-workers/recallweave/docs/task-contracts.md:27): every citation resolves to real physical vault lines.
- [docs/task-contracts.md](/Users/josh/particle-workers/recallweave/docs/task-contracts.md:483): a citation alongside a passage makes it attributed evidence.
- [CHANGELOG.md](/Users/josh/particle-workers/recallweave/CHANGELOG.md:18): the citation is described as “attributing” the quoted passage.

The stale-index case further limits the claim: index-only resolution proves that coordinates existed in the indexed snapshot, not that they still identify the current vault’s physical lines. The existing stale-index test at [test_contract_bounds.py](/Users/josh/particle-workers/recallweave/tests/test_contract_bounds.py:285) records `indexed_at` but does not test or qualify this attribution claim.

This is an unresolved High evidence-integrity defect and blocks promotion.

## 2. Missing adversarial tests

- Replace a persisted connection passage with unrelated text while retaining an exactly resolving citation; require export to fail closed.
- Change only the persisted heading while keeping the citation and passage valid; either verify it against the indexed section or explicitly define it as unverified metadata.
- Exercise matching for both ordinary and 500-character truncated connection passages, including the ellipsis convention.
- Modify the vault after indexing and test the documented semantics explicitly: citations resolve to the indexed snapshot, not necessarily the current vault.
- Build a budget-truncated export containing duplicate retrieved/connection citations and distinct source/target citations; assert the exact complete inventory of only the connections actually returned.

## 3. Positive findings

- The supplied harness results are green: 390 tests passed normally and with `ResourceWarning` promoted to an error; `compileall` also passed.
- Unresolvable, malformed, inverted, and non-citation connection references now fail closed.
- Citation matching requires exact indexed section bounds rather than containment.
- A passage without any citation is rejected.
- Failure diagnostics identify database edge IDs without disclosing citation or endpoint paths.
- Excluded endpoints are suppressed before connection validation.
- `provenance.citations` inventories admitted connection citations after retrieved citations, source before target, with deduplication. Budget-truncated, non-admitted connections are correctly absent from that inventory.
- The working tree is clean and matches `origin/foundry/task-contracts`.
- Previously identified Critical/High classes remain closed: Markdown injection, evidence-field boundary collisions, structural-absence forgery, schema-identity regression, hard-budget overflow, excluded-content leakage, malformed emitted evidence, endpoint-path disclosure, and missing connection citations. The new passage/citation correspondence defect is the sole unresolved Critical/High class found in this cycle.

## 4. Ranked recommended fixes

1. Resolve each connection citation to the indexed section row and verify that the sanitized persisted passage equals that section’s expected bounded passage, including the exact truncation/ellipsis rule. Fail closed with the existing content-free edge-ID diagnostic on mismatch.
2. Verify persisted headings against the resolved section or omit them from connection evidence.
3. Replace the arbitrary `"evidence"` success fixture with the actual indexed section passage and add mutation tests proving passage and heading mismatches fail.
4. Rewrite “physical vault lines” claims as “lines in the indexed snapshot,” unless export-time vault reads and freshness verification are intentionally introduced.

## 5. Push safety

The tree is **not safe to push, promote, or merge under this review gate**. It is not safe to merge into protected `main`; the unresolved High evidence-integrity defect requires remediation and another review cycle.