VERDICT: FAIL

## 1. Findings

- **High — Cold Trails persistence leaks vault-relative note paths into `localStorage`, contradicting the privacy contract.**  
  [cold-trails-feedback-store.ts](/Users/josh/particle-workers/recallweave/viewer/app/cold-trails-feedback-store.ts:38) serializes dismissal keys unchanged. [cold-trails.ts](/Users/josh/particle-workers/recallweave/viewer/app/cold-trails.ts:135) constructs those keys directly from node IDs, which normally represent relative note paths. This violates [cold-trails.md](/Users/josh/particle-workers/recallweave/docs/cold-trails.md:218), which promises that persisted feedback records pair hashes rather than paths.

  Concrete reproduction using in-memory storage on the current tree produced:

  ```json
  [["recallweave.cold-trails.dismissed.v1:export:export-privacy-demo",
    "[\"Health/Therapy Notes.md|Legal/Divorce Strategy.md\"]"]]
  ```

  Consequently, sensitive taxonomy survives after the graph leaves browser memory and is visible to any same-origin script or browser-storage inspection. Additionally, `graphFeedbackFingerprint()` places the normalized but otherwise unrestricted `export_id` directly into the storage key at [cold-trails-feedback-store.ts](/Users/josh/particle-workers/recallweave/viewer/app/cold-trails-feedback-store.ts:6), so a path-like untrusted `export_id` can also become persistent metadata.

## 2. Missing adversarial tests

- Dismiss a trail whose node IDs contain sensitive relative paths and assert neither path appears anywhere in the resulting storage key or value.
- Load a graph with a path-like or hostile `export_id` and assert the raw identifier is not exposed in the storage key.
- Exercise the actual `ColdTrailsTour` dismiss and clear controls against injected storage; the current rendered-source test merely asserts function names are present.
- Verify persisted dismissals still match the correct pair after hashing, and cannot bleed between unrelated graphs with reused `export_id` values.
- Test malformed, oversized, and collision-prone persisted arrays with explicit size limits.

## 3. Positive findings

- The cycle-32 remediation correctly asserts `Index claims:` in `AtlasProvenanceChrome.ts`, its location after component extraction.
- The supplied harness report records 511 Python tests passing with one skip, the ResourceWarning run passing, `compileall` passing, and the full viewer build/test suite passing.
- Provenance and privacy chrome have behavioral SSR coverage, including hostile HTML-like claims and conflicting history.
- Rejected viewer file loads retain the prior graph and provenance state.
- Candidate, supporting, and verified evidence remain distinct in the inspected rendering and Cold Trails paths.
- No direct vault-navigation URI was found in runtime viewer code.

## 4. Ranked recommended fixes

1. Hash canonical pair identities before persistence; never serialize raw node IDs or paths.
2. Derive the graph-storage key from a digest of the graph identity rather than embedding raw `export_id`.
3. Add an end-to-end storage test proving sensitive identifiers appear in neither keys nor values.
4. Bound the number and length of loaded dismissal entries before retaining them.

## 5. Push safety

The tree is **not safe to push**. The unresolved privacy violation requires a `FAIL` verdict. The working tree also contains an uncommitted `.beads/interactions.jsonl` change.