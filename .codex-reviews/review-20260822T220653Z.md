VERDICT: FAIL

## 1. Findings

### High — discovery-candidate evidence accepts empty or fabricated `shared_terms`

The validation boundary verifies connection passages but does not validate the evidence that makes an edge a `discovery_candidate`.

- [contract.py](/Users/josh/particle-workers/recallweave/src/recallweave/contract.py:116) checks only that `shared_terms` is a list. It does not require string elements, non-emptiness, the architecture’s minimum of two terms, or correspondence with the indexed term data.
- [contract.py](/Users/josh/particle-workers/recallweave/src/recallweave/contract.py:291) silently filters non-string elements. Thus malformed persisted input such as `[1, {...}]` becomes `[]`, which validation accepts.
- [contract.py](/Users/josh/particle-workers/recallweave/src/recallweave/contract.py:296) similarly accepts arbitrary or empty persisted `method` and `explanation` strings without authenticating them.
- [index.py](/Users/josh/particle-workers/recallweave/src/recallweave/index.py:448) creates a candidate only with at least two shared terms and emits the actual ranked terms at [index.py](/Users/josh/particle-workers/recallweave/src/recallweave/index.py:494).
- [ARCHITECTURE.md](/Users/josh/particle-workers/recallweave/ARCHITECTURE.md:81) promises local TF-IDF evidence with at least two informative shared terms.
- [docs/task-contracts.md](/Users/josh/particle-workers/recallweave/docs/task-contracts.md:168) presents `shared_terms` as the evidence carried by a lexical discovery candidate.

Read-only reproduction on the current tree:

```text
{"shared_terms": []}
=> {"shared_terms": []}
connection_evidence_is_well_formed(...) => True

{"shared_terms": [1, {"vault": "secret"}]}
=> {"shared_terms": []}
connection_evidence_is_well_formed(...) => True

{"shared_terms": ["fabricated-term"], "method": "", "explanation": ""}
=> unchanged
connection_evidence_is_well_formed(...) => True
```

Consequently, a persisted edge can claim an arbitrary projected shared term—or no shared terms at all—and still export as a valid `discovery_candidate`. The citation checks authenticate the passages, but not the asserted relationship between them. This blurs candidate evidence into an unauthenticated database assertion and is an evidence-integrity violation.

This is the fourth “one level below” regression: cycle 19 completed the nested side shape, while the sibling `shared_terms`, `method`, and `explanation` payload remains only superficially typed.

### Medium — the suite explicitly blesses behavior that contradicts its own invariant

[test_contract_document.py](/Users/josh/particle-workers/recallweave/tests/test_contract_document.py:398) declares that emitted `shared_terms` can never be empty and says it carries the terms by which the candidate was found. But [test_contract_document.py](/Users/josh/particle-workers/recallweave/tests/test_contract_document.py:597) explicitly expects `{"shared_terms": []}` to be well formed.

The positive builder-shape test covers only freshly generated indexes. It does not exercise the persisted-evidence boundary where `_edge_evidence` converts malformed terms to an accepted empty list. The claimed invariant therefore exists in the suite but is unenforced at the public validation boundary—the exact recurring defect class called out in the cycle brief.

## 2. Missing adversarial tests

- Predicate-level rejection of empty `shared_terms`.
- Predicate-level rejection of non-string terms and fewer than two terms.
- Full-export test where persisted `[1, {...}]` is filtered to `[]`; export must fail closed rather than silently normalize corruption.
- Full-export test with two fabricated strings that are not shared indexed terms.
- Mutation test proving removal of the term-content/authenticity check fails independently of fresh-index builder tests.
- Tests pinning whether `method` and `explanation` must reproduce the indexer’s canonical values or should be removed from the exported evidence surface.

## 3. Positive findings

- The supplied harness passed all 398 tests normally and with `ResourceWarning` treated as an error; `compileall` also passed.
- Cycle 19’s complete-side fix holds: both evidence sides require exactly `citation`, `heading`, `passage`, and `truncated`.
- All four side leaves are compared unconditionally against the indexed snapshot.
- Fabricated passages, headings, citations, and truncation flags remain rejected.
- Connection citations remain complete, deduplicated, and ordered in provenance.
- Excluded endpoints are suppressed before validation, and error diagnostics remain free of vault paths and evidence content.
- Markdown values remain inert and structurally separated.
- The previously reported Critical/High classes remain closed: Markdown injection routes; field-boundary collisions; schema-identity and falsy/null compatibility regressions; hard-budget overflow; structural-absence forgery; projection injectivity failures; false evidence-omission documentation; malformed side emission; excluded-content leakage; endpoint-path disclosure; missing/fabricated connection citations; and fabricated passages behind genuine coordinates.
- `git diff --check` passed. The only working-tree change is passive `.beads/issues.jsonl` churn.

## 4. Ranked recommended fixes

1. Authenticate discovery evidence against indexed term data: require at least two distinct strings and verify they are genuinely shared terms for both endpoint notes. Ideally also verify their canonical ordering/bound.
2. Fail closed on malformed persisted elements rather than filtering them into a valid-looking empty list.
3. Define and enforce the contract for `method` and `explanation`: reproduce canonical indexer values, or omit them from exported evidence if they are not trustworthy.
4. Add predicate-level and full-builder mutation tests independent of fresh-index happy paths.
5. Reconcile the contradictory empty-list test with the suite’s stated never-empty invariant.

## 5. Push safety

The tree is **not safe to merge into protected `main`**, promote, release, or deploy. The unresolved High evidence-integrity defect blocks promotion.

Under this review gate, it is also **not safe to push as an approved revision**. A non-approved durability checkpoint is a separate repository-policy decision, not a passing review.