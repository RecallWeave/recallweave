VERDICT: PASS WITH FIXES

## 1. Findings

### Low — the portability guard does not cover all three hostile-filename fixtures

The two tests in [test_contract_markdown_injection.py](/Users/josh/particle-workers/recallweave/tests/test_contract_markdown_injection.py:220) share `HOSTILE_VAULT_FILENAME`, and the portability guard validates that constant at lines 1280–1315.

However, [test_contract_markdown_ast.py](/Users/josh/particle-workers/recallweave/tests/test_contract_markdown_ast.py:822) duplicates the filename as a string literal. That is the third test that previously failed on Windows. Reintroducing a Windows-invalid character in this copy would bypass `HostileFilenamePortabilityTest` and again fail only on Windows.

Current failure scenario: none—the duplicated value is presently portable. This is a test-integrity gap and makes the stated “shared as one constant across the three tests” claim inaccurate.

Recommended change: define the fixture in a small shared test helper module and import it from both test files, or apply the same portability assertion directly to the AST test’s filename.

No Critical, High, or Medium findings were identified.

## 2. Missing adversarial tests

- Mutation test proving that changing the filename used by `EndToEndHostileVaultFilenameTest` to a colon-bearing value is caught locally by the portability guard.
- Case-insensitive-filesystem coverage for vaults containing paths differing only by case. The current macOS runner may or may not expose this depending on its filesystem configuration; Linux generally will not.
- Windows junction tests that demonstrably exercise an actual junction rather than skipping when ordinary symlink creation is unavailable. The destination protocol explicitly promises junction refusal.
- A line-ending matrix that builds equivalent indexed evidence from LF and CRLF files and compares the documented deterministic output byte-for-byte.
- Viewer validation on Windows. The Python matrix is cross-platform, but the `viewer` job runs only on Ubuntu even though it contains a Windows static-asset regression test.

## 3. Positive findings

- The harness results in `.codex-reviews/suite-20260823T011603Z.txt` show:

  - 433 tests passed normally.
  - 433 tests passed with `ResourceWarning` promoted to an error.
  - `compileall` completed cleanly.

- There is no `src/` change since the cycle-30 PASS.
- The product-affecting post-PASS delta is limited to viewer dependency upgrades. The other changes are tests, changelog/review/handoff documentation, and removal of the promotion checkpoint marker.
- The merge from `main` carries only `CHANGELOG.md`, `viewer/package.json`, and `viewer/package-lock.json`.
- The replacement hostile filename retains live Image and Link syntax while avoiding Windows-reserved characters, C0 controls, and trailing space/dot.
- The two injection tests using the shared constant still verify actual inert rendering, rather than merely checking key presence.
- The CI workflow runs Python 3.11–3.13 on Windows, macOS, and Linux and requires the CommonMark test dependency.
- `git diff --check` is clean and the working tree is clean.
- The package lock and manifest move together. I could not independently query the npm advisory service because network access was unavailable, so the claimed zero-advisory result must be confirmed by the required `viewer` CI job.

All historical Critical and High classes from cycles 1–30 remain closed:

- Markdown injection through inline, block, multiline-citation, bare-CR, indented-structure, and handling-statement routes.
- Hard character-budget overflow.
- Falsy-citation and schema-identity compatibility regressions.
- Evidence-boundary collisions and projected-field injectivity failures.
- False connection-evidence omission claims.
- Vacuous or unenforced well-formedness.
- Structural-absence forgery.
- Excluded-content leakage and error-route path disclosure.
- Missing or unauthenticated citations and incomplete provenance inventory.
- Fabricated passages behind valid coordinates.
- Incomplete evidence-side shapes and truncation claims.
- Fabricated discovery evidence.
- Unauthenticated persisted edge classification.
- Fabricated authored links, resolver ambiguity, and lost fenced-code parser state.
- Unbound heading coordinates, levels, and source text.
- Operator-authored glosses misclassified as cited passages.

No prior review recorded a Critical finding, and none is introduced here.

## 4. Ranked recommended fixes

1. Share `HOSTILE_VAULT_FILENAME` across both test modules so the portability guard covers all three original regression tests.
2. Add explicit case-insensitive-path and Windows-junction CI coverage.
3. Consider running the viewer’s production asset test on Windows as well as Ubuntu.

## 5. Push safety

The tree is safe to push and, from the reviewed code and artifact perspective, safe to merge into protected `main`. The Low test-maintainability finding does not affect current product behavior or the present fixture’s portability.

The merge should still wait for the required GitHub matrix—including Windows and both viewer audits—to finish green. I could not independently retrieve those live check results because network access was unavailable.