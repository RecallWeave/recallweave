VERDICT: PASS WITH FIXES

## 1. Findings

### Medium — Tie-breaker regression still does not prove the production `ORDER BY` is load-bearing

In [tests/test_contract_exclusions.py](/Users/josh/particle-workers/recallweave/tests/test_contract_exclusions.py:529), `test_tied_score_export_order_follows_edge_id_tiebreaker` permutes the `INTEGER PRIMARY KEY` edge IDs and asserts that the two exported orders differ.

This does not distinguish the intended query at [src/recallweave/contract.py](/Users/josh/particle-workers/recallweave/src/recallweave/contract.py:819) from the same query with its `ORDER BY` removed. SQLite commonly scans this table in rowid order, and `INTEGER PRIMARY KEY` aliases rowid. Therefore:

- with `ORDER BY …, e.id`, permuting IDs changes output order;
- without the entire `ORDER BY`, the implicit rowid scan can also change output order;
- the regression passes in both cases.

The test proves that edge IDs influence current output, but not that the explicit deterministic-ordering clause prevents dependence on unspecified SQLite row order. This is materially the same coverage defect reported in cycle 24e.

The harness results are otherwise green: both suite runs report 478 tests passing with one skip, including the ResourceWarning-strict run; `compileall` also passed.

## 2. Missing adversarial tests

- A mutation test that removes or patches out `ORDER BY e.is_verified DESC, e.score DESC, e.id` and demonstrates failure.
- Alternatively, a database/query-plan perturbation where implicit row order demonstrably differs from `e.id` order, followed by an assertion of the exact expected exported order—not merely inequality between two outputs.
- Separate mutations removing each ordering component (`is_verified`, `score`, and `id`) so every promised ordering key is proven load-bearing.

## 3. Positive findings

- The two fixture-portability defects from cycle 24e are covered: constant f-string formatting now uses Python’s formatting machinery, and `Path.touch()` is scanned.
- The handoff now distinguishes cycle 24d’s PASS from the later remediation review instead of claiming the remediated tree was already approved.
- Production code is unchanged from the previously passing implementation; the current remediation delta is confined to tests, review/handoff records, and Beads export state.
- The production connection query explicitly orders by verification status, score, and edge ID.
- The working tree is clean and matches `origin/foundry/steward`.
- No new security, privacy, evidence-integrity, schema, or public-output regression was identified.

## 4. Ranked recommended fixes

1. Replace the edge-ID permutation regression with a mutation test that actually fails when the production `ORDER BY` is removed.
2. Assert the exact intended connection order, including verified status, score, and ID tie-breaking.
3. Re-run the complete suite and final review on the resulting commit.

## 5. Push safety

The tree is not safe to push as an approved promotion revision or merge into `main`. No Critical or High defect was found, but the required final PASS has not been earned because the cycle-24e deterministic-order coverage finding remains unresolved.