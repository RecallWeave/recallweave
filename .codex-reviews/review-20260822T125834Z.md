VERDICT: FAIL

## 1. Findings

### High — Markdown privacy/projection documentation falsely says connection evidence is omitted

[docs/task-contracts.md](/Users/josh/particle-workers/recallweave/docs/task-contracts.md:293) claims its list is the exact projected field set, while lines 335–343 explicitly say connection `score` and `evidence` are omitted. In reality, [contract_markdown.py](/Users/josh/particle-workers/recallweave/src/recallweave/contract_markdown.py:276) renders:

- `connections[].evidence_class`
- `connections[].score`
- source/target evidence citations, headings, and passages
- `connections[].evidence.shared_terms[]`

These fields are also absent from `PROJECTED_FIELDS` in [test_contract_projection.py](/Users/josh/particle-workers/recallweave/tests/test_contract_projection.py:129). Consequently, the documentation/test agreement test is self-referential: both lists are identically incomplete and therefore pass despite contradicting the renderer.

Failure scenario: a consumer relies on the documented projection boundary when deciding whether a Markdown artifact is safe to share, but the artifact contains connection evidence passages and note-derived terms that the documentation promises are omitted. This is a privacy/disclosure-contract violation.

### High — injectivity remains false for missing keys versus explicit null, and for missing exclusion collections versus empty collections

The documentation states that an absent field renders `None recorded.` and that an absent collection differs from an empty list at [docs/task-contracts.md](/Users/josh/particle-workers/recallweave/docs/task-contracts.md:272). It further claims line-ending normalization is the single injectivity exception at line 347.

The implementation does not preserve key presence consistently:

- Acceptance, constraint, prior-decision, retrieved-context, and connection fields use `dict.get()`, collapsing missing keys and explicit `None`: [contract_markdown.py](/Users/josh/particle-workers/recallweave/src/recallweave/contract_markdown.py:170), lines 192–195, 215–218, and 268–278.
- Connection evidence leaves likewise use `get()` at lines 233–246.
- Missing exclusion collection keys are skipped, while empty lists emit zero elements; both therefore disappear at lines 287–301.
- Missing and null `provenance.citations` both take the null-marker path at lines 340–346.

Read-only reproduction against the current tree produced:

```text
exclusions.paths missing vs [] True 320 320
task.id missing vs None False 320 341
acceptance id missing vs None True 407 407
constraint citation missing vs None True 455 455
connection score missing vs None True 1054 1054
provenance citations missing vs None True 343 343
```

`True` means byte-identical Markdown. Thus the narrowed claim is still false: line-ending normalization is not the only collision.

The new test does not exercise key absence. It sets fields to `None` and calls that “absent” at [test_contract_projection.py](/Users/josh/particle-workers/recallweave/tests/test_contract_projection.py:553). That proves null-versus-empty, not missing-key-versus-null/empty.

## 2. Missing adversarial tests

- Remove each projected key with `pop()` and compare against explicit `None`, `""`, `[]`, `0`, or `False`, as appropriate.
- Test missing versus empty for every exclusion collection.
- Test missing versus null for every per-item field in acceptance criteria, constraints, prior decisions, retrieved context, connections, and connection evidence.
- Derive the projected field inventory from actual renderer behavior or a production constant, rather than comparing two manually synchronized incomplete lists.
- Add injectivity tests for every newly rendered connection field: `evidence_class`, `score`, all six bilateral evidence leaves, and `shared_terms[]`.
- Assert that documentation’s “not projected” list has no overlap with fields actually rendered.
- Extend line-ending normalization cases across all projected string leaves, including connection evidence passages and shared terms.

## 3. Positive findings

- The supplied harness result reports all 356 unit tests passing and `compileall` succeeding.
- Explicit `None` is now distinguishable from empty strings, zero, and false for the fields covered by the new matrix.
- Dynamic fence lengths and CRLF/bare-CR normalization continue to keep untrusted Markdown content inert.
- Connection evidence fields are individually labeled and fenced; the evidence-class boundary is visually preserved.
- No files were modified during this review.

## 4. Ranked recommended fixes

1. Define the exact projection boundary truthfully: add all rendered connection fields to the documented and tested projected set, or stop rendering them if omission is the intended privacy contract.
2. Decide and encode missing-key semantics consistently. Given the injectivity promise, use membership checks and a distinct trusted marker for a missing key; do not equate missing with explicit null.
3. Render an explicit marker for present empty collections, or narrow the injectivity contract to define collection presence/emptiness equivalence precisely.
4. Replace the current `None`-as-“absent” test with a three-way missing/null/empty matrix.
5. Add a test that detects disagreement between renderer output and the documented projected/omitted sets.

## 5. Push safety

The tree is **not safe to push**. Two unresolved High findings remain: a privacy-relevant false projection claim and a reproducible violation of the documented injectivity contract.