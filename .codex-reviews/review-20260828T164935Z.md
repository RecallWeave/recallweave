VERDICT: PASS WITH FIXES

1. **Findings**

- **Medium — The provenance-chrome test does not verify GraphExplorer integration.**  
  [viewer/tests/graph-data.test.mjs](/Users/josh/particle-workers/recallweave/viewer/tests/graph-data.test.mjs:664) behaviorally tests `formatExportHistoryDetail`, but no test renders or exercises [GraphExplorer.tsx](/Users/josh/particle-workers/recallweave/viewer/app/components/GraphExplorer.tsx:339) with loaded export history. Removing the call to `formatExportHistoryDetail` from GraphExplorer would leave the new formatter tests green while provenance chrome silently disappeared. This falls short of the cycle’s claimed behavioral coverage and the remediation requirement that the test fail when the repaired wiring is removed.

2. **Missing adversarial tests**

- Render GraphExplorer with valid export history and assert the visible provenance chrome contains the export ID, first/prior-export classification, and counters without conflict wording.
- Render it with conflicted history and assert the visible chrome includes “export history conflicts with loaded graph.”
- Render omitted `previous_content_hash` through `normalizeGraph` and then GraphExplorer, verifying the conflict reaches the actual UI rather than only the formatter.
- Mutation check: remove or bypass the formatter call at `GraphExplorer.tsx:339–341`; the integration test must fail.

3. **Positive findings**

- The supplied harness results are green: 511 Python tests with one skip, the strict `ResourceWarning` run, `compileall`, viewer production build, and all viewer tests passed.
- The extraction is behavior-preserving relative to base `37eea2a`; the displayed strings, ordering, separators, and conditional conflict wording are unchanged.
- Omitted `previous_content_hash` is tested behaviorally and becomes `previous_content_hash: null` with `claim_conflict: true`.
- Valid and conflicted formatter results are distinguished without source-regex assertions.
- Export-history fields pass through normalization before formatting; the extracted formatter does not create a new raw-input rendering boundary.
- `git diff --check` passed, and the working tree is clean.
- No security, privacy, evidence-integrity, deterministic-output, or backward-compatibility defect was found in the reviewed change.

4. **Ranked recommended fixes**

1. Add a GraphExplorer-level behavioral test covering valid, conflicted, and omitted-prior history.
2. Add the formatter-call removal mutation to the review gate or document a manual mutation check.

5. **Push safety**

The clean `foundry/steward` checkpoint is safe to push under the cycle’s allowance for Medium-only findings. Do not promote or merge it as a fully clean gate until the missing UI integration coverage is added.