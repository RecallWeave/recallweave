VERDICT: FAIL

## 1. Findings

- **High — Atlas accepts an omitted required `previous_content_hash` as an explicit `null`.** In [viewer/app/graph-data.ts:290](/Users/josh/particle-workers/recallweave/viewer/app/graph-data.ts:290), `raw.previous_content_hash` returns `undefined` when absent. Lines 292–295 explicitly exclude `undefined` from `priorMalformed`, allowing a producer-shaped first-export history to receive `claim_conflict: false`. This violates the required v2 shape in [docs/json-output.md:347](/Users/josh/particle-workers/recallweave/docs/json-output.md:347) and mirrors the Python defect remediated in cycle 29d.

  Reproduction against the current tree:

  ```text
  {"export_id":"x","previous_content_hash":null,"node_content_hashes_changed":0,"node_content_hashes_unchanged":0,"nodes_added":1,"nodes_removed":0,"claim_conflict":false}
  ```

  The input omitted `previous_content_hash` entirely. Atlas nevertheless normalizes it into a clean, trusted history claim. [viewer/app/cold-trails.ts:808](/Users/josh/particle-workers/recallweave/viewer/app/cold-trails.ts:808) subsequently treats any history with `claim_conflict === false` as trusted. The UI also presents it without the conflict warning generated at [viewer/app/components/GraphExplorer.tsx:346](/Users/josh/particle-workers/recallweave/viewer/app/components/GraphExplorer.tsx:346).

## 2. Missing adversarial tests

- Remove `previous_content_hash` from an otherwise valid v2 graph and assert `claim_conflict: true` or rejection.
- Independently omit every required `export_history` field in the viewer’s TypeScript normalizer, not only the Python predecessor validator.
- Mutation-test changing the viewer’s explicit presence check back to an `undefined`-tolerant lookup.
- Verify an incomplete history cannot be treated as trusted by Cold Trails or displayed without a conflict warning.

## 3. Positive findings

- The recorded harness artifact reports 511 Python tests passing with one skip, a ResourceWarning-clean repeat, successful `compileall`, and all viewer build/tests passing.
- The Python remediation at [src/recallweave/viewer.py:195](/Users/josh/particle-workers/recallweave/src/recallweave/viewer.py:195) now explicitly requires all six history keys, including nullable `previous_content_hash`.
- The Python regression iterates over every required history-field omission and exercises forced replacement through the public exporter.
- Forced replacement ignores incomplete predecessors and emits `previous_content_hash: null`.
- `git diff --check 1fd322a..HEAD` is clean.
- The working tree is clean.
- No files were modified during this review.

## 4. Ranked recommended fixes

1. In `parseExportHistory`, distinguish an absent `previous_content_hash` property from an explicitly present JSON `null`; mark omission conflicted or reject the history.
2. Add field-omission tests covering all required history fields in `viewer/tests/graph-data.test.mjs`.
3. Add an integration test proving incomplete history is neither trusted by Cold Trails nor presented as conflict-free.
4. Mutation-test removal of the TypeScript key-presence guard.

## 5. Push safety

The tree is **not safe to push**. Although all recorded gates pass and the Python remediation is correct, an unresolved High evidence-integrity defect remains in the browser consumer.