VERDICT: FAIL

## 1. Findings

### High — Markdown projection destroys evidence boundaries

[src/recallweave/contract_markdown.py:179](/Users/josh/particle-workers/recallweave/src/recallweave/contract_markdown.py:179) concatenates a constraint/prior-decision statement and citation into one unlabeled fenced block. This makes distinct evidence classes render byte-for-byte identically:

- Operator-authored statement: `"Author asserted.\nVault.md:7-8"`, citation `None`
- Cited statement: `"Author asserted."`, citation `"Vault.md:7-8"`

Reproduction output:

```text
constraint True
```

This is an evidence-integrity violation: a reader cannot distinguish what the operator asserted from the citation attached as supporting evidence. The base `da58ccd` renderer preserved that boundary through distinct formatting.

The same collision exists at:

- [contract_markdown.py:202](/Users/josh/particle-workers/recallweave/src/recallweave/contract_markdown.py:202): retrieved-context citation and passage.
- [contract_markdown.py:154](/Users/josh/particle-workers/recallweave/src/recallweave/contract_markdown.py:154): acceptance-criterion ID and statement.

Reproduction:

```text
constraint True
retrieved True
acceptance True
```

Thus the Markdown is not a faithful projection of the canonical document. The documentation explicitly describes this merging at [docs/task-contracts.md:258](/Users/josh/particle-workers/recallweave/docs/task-contracts.md:258), so the specification shares the defect.

### Medium — Projection-completeness test checks presence, not preservation

[tests/test_contract_markdown.py:221](/Users/josh/particle-workers/recallweave/tests/test_contract_markdown.py:221) uses `assertIn` for distinctive values. It does not verify:

- Field boundaries or labels
- Multiplicity
- Ordering within an item
- Injectivity between materially different documents
- Association of citations with the correct statements
- Evidence-class distinctions

Consequently, it passes despite all three demonstrated semantic collisions. This is the repository’s known weak-test pattern: documented keys/values are present, but their meaning and stable association are not tested.

## 2. Missing adversarial tests

- Operator-authored constraint with a citation-looking final line versus cited constraint with the same citation; outputs must differ.
- Multiline statement versus statement-plus-citation boundary collision for both constraints and prior decisions.
- Retrieved citation containing a newline versus passage beginning with that same suffix; outputs must differ.
- Acceptance ID/statement boundary collision.
- Two adjacent provenance citations versus one multiline citation.
- Connection values containing lines such as `target:`, `kind:`, or `verified:`; field identity must remain unambiguous.
- Projection test that parses labeled fenced fields and reconstructs their semantic associations, rather than searching for substrings.
- Mutation test removing or reassigning one field while leaving its value elsewhere; completeness must fail.

## 3. Positive findings

- The harness report at `.codex-reviews/suite-20260822T051850Z.txt` records all 326 tests passing and `compileall: OK`.
- Top-level `schema_version` is restored and routed through an inert fence.
- Parser-backed tests are no longer silently skipped by the review script or CI.
- Hostile document strings remain inside dynamically sized code fences; the trusted CommonMark structure remains invariant.
- The eight numbered sections retain their order.
- No runtime dependency was introduced; `mistletoe` is confined to the test extra.
- The review found no excluded-vault-content leakage or destination-protocol regression in the inspected cycle changes.

## 4. Ranked recommended fixes

1. Preserve every semantic boundary inside fences using trusted external labels or separate labeled fences—for example, distinct `Statement:` and `Citation:` labels/fences. Never infer the boundary from line position.
2. Add collision tests proving materially different canonical documents cannot produce identical Markdown when evidence meaning differs.
3. Replace substring-based completeness checks with structured assertions covering field identity, association, multiplicity, and order.
4. Update `docs/task-contracts.md` so it no longer promises or endorses ambiguous merged fields.
5. Apply the same explicit-boundary treatment to retrieved context, acceptance criteria, provenance citations, and connection fields.

## 5. Push safety

The tree is **not safe to push**. The unresolved High evidence-integrity defect requires a `FAIL` verdict.