VERDICT: PASS WITH FIXES

## 1. Findings

### Medium — Valid links in bodyless headings cannot be authenticated

[src/recallweave/parser.py:328](/Users/josh/particle-workers/recallweave/src/recallweave/parser.py:328) records a heading as a `Section` only when its following body is non-empty (`if text:` at line 338). However, [src/recallweave/parser.py:375](/Users/josh/particle-workers/recallweave/src/recallweave/parser.py:375) extracts links from every heading line independently.

Consequently, a note such as:

```markdown
# Searchable section

zephyr searchable body

## Related [[Target]]
```

produces a genuine authored edge for the final heading, but no `sections` row holding that heading’s `heading_line` and `heading_level`. During export, [src/recallweave/contract.py:420](/Users/josh/particle-workers/recallweave/src/recallweave/contract.py:420) cannot find the heading coordinate and rejects the genuine edge as malformed.

This does not admit forged evidence—the exporter fails closed—but it rejects indexer-produced evidence and contradicts the general claim that heading links are fully bound. The existing positive test always gives the heading a non-empty body.

I could not create the database fixture under the mandated read-only review environment. The failure follows directly from the parser/indexer control flow above.

### Medium — CHANGELOG simultaneously claims heading coordinates are unbound and bound

[CHANGELOG.md:31](/Users/josh/particle-workers/recallweave/CHANGELOG.md:31) still says that for heading links “the coordinate and heading level are not” bound and that the docs disclose this gap. Lines 36–40 immediately state the corrected behavior: both are now recorded and bound.

The first statement is obsolete and false on the current implementation. This is especially misleading in a security/evidence-integrity changelog entry.

No Critical or High finding remains open.

## 2. Missing adversarial tests

- A link in a terminal heading with no following body.
- A link in a heading followed only by blank lines.
- A bodyless linked heading following a searchable earlier section, proving the note can be selected and its genuine edge exported.
- A direct assertion that the CHANGELOG no longer contains the obsolete “coordinate and heading level are not” disclosure.
- A capability-probe fixture where the columns exist but heading metadata is absent for an indexer-produced heading, distinguishing format capability from complete per-row data.

## 3. Positive findings

- The harness results in `.codex-reviews/suite-20260823T000029Z.txt` show all 420 tests passing normally and with `ResourceWarning` promoted to an error; `compileall` is clean.
- The working tree is clean.
- The authorship/support split is the correct evidence boundary. Operator wording remains `authored_by_operator`; citations and passages are separately attributed support. No unverifiable semantic-support inference was introduced.
- `cited_passage` statements are constructed from the cited passage itself.
- Statement and supporting-passage truncation are represented separately.
- The Markdown projection emits the supporting passage in its own labeled fence, preserving attribution and inert rendering.
- The projected/omitted partition was updated for the new evidence shape.
- Heading-link verification binds coordinate, level, complete reconstructed heading text, link kind, target, and unique resolution when heading metadata exists.
- The named line-1, line-999999, altered-level, and identical-heading-coordinate forgeries are covered.
- Direct capability probing is preferable to changing the public receipt version. Refusing pre-capability indexes with a content-free re-index diagnostic is coherent and backward-compatible at the receipt layer.
- Failure diagnostics continue to identify malformed edges by database ID without exposing vault paths or passages.
- Review of the previous cycle artifacts and current implementation found the prior Critical/High classes closed: Markdown injection, exclusion/error-route leakage, malformed-evidence normalization, incomplete field partitioning, passage attribution, missing truncation leaves, fabricated candidate evidence, forged edge envelopes, unbound authored-link parsing/resolution, fenced-link confusion, operator-gloss misclassification, and heading coordinate/level forgery.

## 4. Ranked recommended fixes

1. Persist heading metadata independently of whether the heading has a non-empty body, then add the bodyless-heading tests above.
2. Remove the obsolete unbound-coordinate clause from `CHANGELOG.md`.
3. Add a test checking the relevant documentation claims semantically, rather than only checking key or phrase presence.

## 5. Push safety

The clean, green tree is safe to push to the non-approved integration checkpoint.

It is **not yet safe to merge into protected `main`** because the two Medium findings remain. Once they are corrected and the suite remains green, nothing identified in this review would block promotion.