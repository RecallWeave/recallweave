VERDICT: FAIL

## 1. Findings

- **High — malformed exports are still accepted as export-history predecessors.** [`src/recallweave/viewer.py:183`](/Users/josh/particle-workers/recallweave/src/recallweave/viewer.py:183) validates only schema name, nonempty unique string IDs, and whether `content_hash` is a string. It accepts nodes missing required v1/v2 fields and accepts arbitrary hash strings, contradicting the v2 contract at [`docs/json-output.md:328`](/Users/josh/particle-workers/recallweave/docs/json-output.md:328). Those malformed nodes then produce a non-null predecessor digest and factual removal counts.

  Reproduction output:

  ```text
  missing_fields [{'id': 'a.md'}] ... previous_content_hash='fc3e...' nodes_removed=1
  bad_hash [{'id': 'a.md', 'content_hash': 'not-a-sha256'}] ... previous_content_hash='ae6e...' nodes_removed=1
  ```

  Thus the claimed cycle-29 remediation is incomplete, and `export_history` can describe a malformed file as a prior approved export. This is an evidence-integrity/reproducibility failure.

- **Medium — valid empty graph exports are rejected as predecessors.** [`src/recallweave/viewer.py:198`](/Users/josh/particle-workers/recallweave/src/recallweave/viewer.py:198) rejects every empty `nodes` array. Empty graphs are supported elsewhere (`empty_graph` privacy profile), and the schema does not prohibit them. Replacing a valid empty export therefore loses its predecessor digest:

  ```text
  valid_empty None ... previous_content_hash=None nodes_added=1
  ```

  The prior implementation produced the SHA-256 digest of the empty aggregate. This is an unapproved compatibility regression in export-history behavior.

## 2. Missing adversarial tests

- A recognized v2 predecessor containing a unique string ID but missing required node fields must be rejected.
- A v2 predecessor with empty, malformed, uppercase, short, or non-hex `content_hash` must be rejected; `null` must remain accepted for legacy indexes.
- A structurally valid empty v1/v2 graph must be accepted as a predecessor and produce the deterministic empty aggregate digest.
- The Parallel test should directly establish that the same pair receives Parallel, Unwritten, and Distant classifications before selection. The current assertion at [`viewer/tests/cold-trails.test.mjs:1996`](/Users/josh/particle-workers/recallweave/viewer/tests/cold-trails.test.mjs:1996) accepts either an early Unwritten/Distant selection or a Parallel selection, so it does not prove co-typing or reservation behavior independently.
- Mutation coverage should remove each predecessor-validation check independently and confirm the malformed-history test fails.

## 3. Positive findings

- The supplied harness report records 510 Python tests passing with one skip, including the ResourceWarning run; `compileall` passed.
- All viewer build/tests passed: 7 rendered-HTML, 21 graph-data, and 30 Cold Trails tests.
- Citation-backed candidates are no longer globally suppressed merely because passage text is absent; candidate eligibility still requires bilateral citations matching the endpoint paths.
- Parallel candidates are co-typed with Unwritten and Distant in [`viewer/app/cold-trails.ts:595`](/Users/josh/particle-workers/recallweave/viewer/app/cold-trails.ts:595), and parallel pairs are excluded from the two early generic slots at [`viewer/app/cold-trails.ts:1129`](/Users/josh/particle-workers/recallweave/viewer/app/cold-trails.ts:1129).
- Signed Drift duration rejects `modified_at` preceding `created_at`.
- The branch is clean and matches `origin/foundry/steward`; `git diff --check` found no whitespace errors.
- No working-tree files were modified during review.

## 4. Ranked recommended fixes

1. Validate a predecessor as a complete viewer document, including required node fields and schema-appropriate hash syntax, before deriving any history claims.
2. Distinguish a valid empty node list from an invalid predecessor; hash the valid empty aggregate deterministically.
3. Add focused tests for malformed-but-unique nodes, hash variants, and valid empty predecessors.
4. Expose or directly test classification independently of selection so the Parallel co-typing and reservation claims cannot pass through an either/or assertion.

## 5. Push safety

The tree is **not safe to push**. The cycle’s malformed-predecessor remediation remains bypassable and emits false export-history facts.