VERDICT: FAIL

## 1. Findings

- **High — incomplete v2 documents still qualify as approved export-history predecessors.** [`src/recallweave/viewer.py:195`](/Users/josh/particle-workers/recallweave/src/recallweave/viewer.py:195) validates node fields but treats an absent `edges` field as valid at lines 215–217. It also does not require v2’s mandatory `export_history` or other required graph structure. This conflicts with the frozen schema in [`docs/json-output.md:307`](/Users/josh/particle-workers/recallweave/docs/json-output.md:307), where `nodes`, `edges`, and v2 `export_history` form the document contract.

  Reproduction on the current tree:

  ```text
  missing_edges_and_export_history validated= [{'id': 'a.md', ...}]
  history= {
    'previous_content_hash': '9008e442...',
    'nodes_added': 1,
    'nodes_removed': 1
  }

  missing_edges_empty validated= []
  history= {
    'previous_content_hash': 'e3b0c442...',
    'nodes_added': 1,
    'nodes_removed': 0
  }

  edges_wrong_type validated= None
  history= {
    'previous_content_hash': None,
    'nodes_added': 1,
    'nodes_removed': 0
  }
  ```

  Thus omitting `edges` bypasses the check that correctly rejects a wrong-typed `edges` value. A hand-crafted, incomplete file can still generate a predecessor digest and factual added/removed counts. Because `previous_content_hash` promises the digest of a “prior approved export,” this remains an evidence-integrity defect.

- **Low — the working tree is not clean.** `git status --porcelain=v1` reports:

  ```text
  ?? .codex-reviews/review-20260828T131251Z.md
  ```

  The repository’s checkpoint policy permits pushing only a clean tree.

## 2. Missing adversarial tests

- A v2 predecessor with valid nodes but no `edges` key must be rejected.
- A v2 predecessor with `nodes: []` but no `edges` key must be rejected rather than hashed as a valid empty export.
- A v2 predecessor missing mandatory `export_history` and other required top-level structure needs an explicit schema-policy test.
- Mutation coverage should independently remove the required-top-level-field checks and confirm rejection tests fail.
- The valid-empty test should use a complete producer-shaped empty v2 document, not only `{schema_version, nodes, edges}`.

## 3. Positive findings

- The harness report records 511 Python tests passing with one skip, including the ResourceWarning run; `compileall` passed.
- Viewer build and tests passed: 7 rendered-HTML, 21 graph-data, and 30 Cold Trails tests.
- The cycle-29 malformed-node cases are now rejected: missing `id`/`title`/`path`, duplicate IDs, missing v2 hash keys, uppercase, short, empty, and non-hex hashes.
- Lowercase SHA-256 and `null` legacy hashes are handled as documented.
- A valid empty node list with an `edges` array now receives the deterministic SHA-256 empty digest.
- [`classifyCandidateEdgeTypes()`](/Users/josh/particle-workers/recallweave/viewer/app/cold-trails.ts:629) directly demonstrates that a qualifying pair receives Parallel, Unwritten, and Distant classifications.
- Signed Drift duration, citation-backed passage-free candidates, evidence-class separation, and deterministic Cold Trails output remain covered and green.
- `git diff --check 1fd322a..HEAD` found no whitespace errors.
- No files were modified during this review.

## 4. Ranked recommended fixes

1. Validate predecessor documents using a schema-aware, complete top-level contract. At minimum, require `edges` to exist and be a list; for v2, require its mandatory export-history structure.
2. Add rejection tests for missing top-level fields alongside a complete producer-shaped empty-export acceptance test.
3. Add mutation coverage for each predecessor-validity boundary.
4. Resolve or intentionally track the untracked prior-review artifact before pushing.

## 5. Push safety

The tree is **not safe to push**. A High evidence-integrity defect remains, and the working tree is dirty.