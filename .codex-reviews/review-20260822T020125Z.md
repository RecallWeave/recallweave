VERDICT: FAIL

## 1. Findings

### High — Multiline cited-item citations permit live Markdown injection

At [contract_markdown.py:174](/Users/josh/particle-workers/recallweave/src/recallweave/contract_markdown.py:174), citations are processed only by `_citation_inline()`, which removes backticks and collapses whitespace. When the associated statement is multiline, [contract_markdown.py:179](/Users/josh/particle-workers/recallweave/src/recallweave/contract_markdown.py:179) emits that citation directly:

```python
lines.append(f"- {cit_str}")
```

Unlike the single-line branch, it is neither enclosed in a code span nor passed through `_escape_inline()`. This affects both `constraints` and `prior_decisions`.

Concrete reproduction:

```python
document["constraints"] = [{
    "statement": "line one\nline two",
    "citation":
        "![beacon](https://attacker.example/pixel) "
        "<https://attacker.example>",
}]
```

Current output:

```markdown
## 3. Constraints

- ![beacon](https://attacker.example/pixel) <https://attacker.example>
```text
line one
line two
```
```

Probe results:

```text
LIVE_IMAGE True
LIVE_AUTOLINK True
```

The image URL can therefore be fetched by a Markdown viewer, and the autolink remains active. This violates the security promise that vault-derived text is rendered strictly as data.

This is reachable through normal document construction: [contract.py:182](/Users/josh/particle-workers/recallweave/src/recallweave/contract.py:182) builds citations from vault-relative paths, while multiline cited passages become statements at [contract.py:188](/Users/josh/particle-workers/recallweave/src/recallweave/contract.py:188). A hostile Markdown filename can consequently reach this branch.

The defect predates the cycle-4 delta, but remains an unresolved security-boundary violation.

## 2. Missing adversarial tests

- A hostile citation containing an image, ordinary link, autolink, raw HTML, emphasis, and backslash runs, paired with a multiline constraint statement.
- The identical case through `prior_decisions`.
- An end-to-end indexed-vault case using a Markdown filename containing link/image syntax and a multiline cited passage.
- Citation values beginning with already-escaped constructs such as `\![x](url)`, `\\![x](url)`, and longer backslash runs.
- A test that verifies every cited-item branch applies the same citation rendering policy, rather than testing only the single-line code-span branch.

The existing stdlib-only structural strategy can detect this particular defect; a CommonMark dependency is not required to fix or pin it. A parser-backed suite would give stronger semantic assurance, but this finding alone does not justify overturning the project’s zero-dependency policy.

## 3. Positive findings

- The harness-recorded suite passed all 284 tests, and `compileall` succeeded.
- Cycle-4 correctly escapes `handling.statement`, including each line of multiline blockquotes.
- `budget.characters_used`, `budget.character_budget`, and suppressed-count scalars are now position-appropriately escaped.
- Existing backslashes are doubled before metacharacters are escaped, producing an odd final run before the protected metacharacter; already-escaped input does not reopen the tested inline constructs.
- Fenced passage content remains byte-preserved, with the enclosing fence selected to exceed the longest backtick run.
- The new cumulative cited-passage budget test covers several individually fitting passages whose combined size breaches the budget.
- The cycle delta does not introduce whitespace errors according to `git diff --check`.

## 4. Ranked recommended fixes

1. Apply `_escape_inline(cit_str)` in the multiline cited-item branch, or render citations through one shared helper used by both branches.
2. Add hostile multiline-citation tests for both constraints and prior decisions.
3. Add an end-to-end hostile-filename reproduction to prove vault-derived citations cannot create live Markdown.
4. Consider centralizing all renderer interpolations behind position-specific helpers so future branches cannot bypass escaping.

## 5. Push safety

The tree is **not safe to push**. A High-severity Markdown injection path remains open.