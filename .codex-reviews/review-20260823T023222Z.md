VERDICT: FAIL

## 1. Findings

### High — Tag exclusions can silently fail after invisible-character normalization

[src/recallweave/contract_exclusions.py:87](/Users/josh/particle-workers/recallweave/src/recallweave/contract_exclusions.py:87) normalizes vault-side tags with `_clean_tag(...).casefold()` but does not call `sanitize()`. This contradicts the remediation’s stated rule that exclusion matching sanitizes both sides.

The index preserves tag text from frontmatter and stores it without sanitization at [src/recallweave/index.py:350](/Users/josh/particle-workers/recallweave/src/recallweave/index.py:350). Therefore:

1. A note carries frontmatter tag `private<ZWSP>`.
2. The operator specifies the clean exclusion tag `private`.
3. The selector is accepted and emitted as `private`.
4. The indexed probe remains `private<ZWSP>`, so `excludes_tags()` does not match.
5. The note’s passage can enter retrieved context, connections, provenance citations, JSON, and Markdown.
6. The artifact still reports `exclusions.enforced: true`.

This is the same silent exclusion-integrity class as the path defect described in the cycle notes, but the fix covered paths/globs and omitted tags. It is a privacy boundary violation and blocks merge.

I could not create the database fixture under the mandated read-only review environment, but the complete source path is visible: raw frontmatter tag → raw `note_tags.tag` → unsanitized `excludes_tags()` comparison.

### Medium — Connection endpoints bypass the documented metadata sanitizer

[src/recallweave/contract.py:1077](/Users/josh/particle-workers/recallweave/src/recallweave/contract.py:1077) copies `source_path` and `target_path` directly into emitted connections. Unlike retrieved metadata, these values do not pass through `sanitize()`.

This contradicts:

- [docs/task-contracts.md:33](/Users/josh/particle-workers/recallweave/docs/task-contracts.md:33), which says vault-relative paths are sanitized;
- [docs/task-contracts.md:968](/Users/josh/particle-workers/recallweave/docs/task-contracts.md:968), which says every emitted string is sanitized.

An authored link involving a filename containing a bidi override or zero-width character can consequently emit visually spoofed endpoint metadata in JSON and Markdown. Markdown fencing prevents it from becoming live markup, but does not remove the misleading Unicode.

## 2. Missing adversarial tests

- An end-to-end vault fixture whose frontmatter contains `tags: ["private\u200b"]`, with exclusion tag `private`; assert its distinctive passage, path, citation, and connection data are absent from JSON and Markdown.
- The same test using a bidi override in the vault-side tag.
- Direct `ExclusionSet.excludes_tags()` tests proving clean selectors match vault-side tags changed by sanitization.
- Connection tests with bidi and zero-width characters in both source and target paths, asserting every emitted endpoint is sanitized.
- A single table-driven test applying the “clean selector matches sanitized vault-side value” invariant to every matching selector class—path, glob, and tag. The current test covers only paths.
- Mutation checks removing `sanitize()` independently from each vault-side exclusion matcher. The existing remediation test would not detect the tag omission.

## 3. Positive findings

- The supplied harness results are green: 454 tests pass normally and with `ResourceWarning` promoted to an error; `compileall` is clean.
- The working tree is clean.
- The endpoint-binding repair itself is correctly threaded through the connection path:
  - each side is paired with its own persisted endpoint ID at [src/recallweave/contract.py:1136](/Users/josh/particle-workers/recallweave/src/recallweave/contract.py:1136);
  - `_side_attribution_is_authentic()` passes that ID into resolution;
  - `_indexed_side_evidence()` constrains both `notes.id` and the citation path;
  - its memo key is `(note_id, citation)`, preventing cross-endpoint cache reuse.
- Retrieved context is exclusion-filtered before emission.
- Explicit constraint and prior-decision selectors resolve through the exclusion check.
- Connection endpoints are excluded before edge validation, preventing excluded content from escaping through error diagnostics.
- Provenance citations are collected only from accepted constraints, retrieved items, and admitted connections. I found no independent provenance route around endpoint binding.
- Connection evidence verifies the complete citation, heading, passage, and truncation tuple against the indexed endpoint section.
- Failure diagnostics identify edges by database ID and do not interpolate endpoint paths or evidence content.
- Previously reported Critical/High classes remain closed except for the newly identified tag-normalization exclusion breach. In particular, endpoint passage substitution, cross-endpoint cache authentication, malformed evidence emission, fabricated citations/passages, unauthenticated edge envelopes, self-edges, Markdown injection, structural-absence forgery, budget overflow, and duplicate-spec-key override remain closed in the inspected tree.

The review process needs invariant matrices across every representation and selector type—not one representative path case—to catch this class locally. The path remediation asserted “both sides are sanitized,” but the tests proved that only for path matching. A diff review noticed the exact changed surface; the local gate needs mutation or table-driven coverage that enumerates paths, globs, tags, retrieved context, connections, constraints, and provenance independently.

## 4. Ranked recommended fixes

1. Sanitize vault-side tags before removing `#` and case-folding in `ExclusionSet.excludes_tags()`.
2. Add end-to-end distinctive-sentinel leakage tests for invisible and bidi tag variants in both JSON and Markdown.
3. Sanitize `source_path` and `target_path` when constructing emitted connection objects.
4. Add a cross-selector mutation matrix so removing normalization from any one selector class fails the suite.

## 5. Push safety

The tree is not safe to merge into protected `main`. The unresolved tag-exclusion breach can silently disclose excluded note content while claiming enforcement succeeded.

It may be retained on the non-approved checkpoint branch under the project’s durability policy, but it must not be promoted, merged, released, or deployed.