VERDICT: PASS WITH FIXES

## 1. Findings

### Low — portability guard misses single-quoted hostile filename literals

At [tests/test_contract_markdown_injection.py:1333](/Users/josh/particle-workers/recallweave/tests/test_contract_markdown_injection.py:1333), the source scan recognizes only double-quoted literals:

```python
re.findall(r'"([^"\n]*\.md)"', source)
```

A regression such as:

```python
self.hostile_name = '![pixel](x)  [click](evil:payload).md'
```

would bypass the guard, pass locally on macOS, and fail when Windows attempts to create the file. Evaluating the current regex against that mutation returned:

```text
[]
[]
```

The equality assertion at lines 1319–1324 only confirms that the imported module constant equals the shared constant; it does not prove [EndToEndHostileVaultFilenameTest](/Users/josh/particle-workers/recallweave/tests/test_contract_markdown_ast.py:808) assigns that constant to `self.hostile_name`.

This is a test-integrity weakness, not a current product defect. It does not block the merge.

No Critical, High, or Medium findings remain.

## 2. Missing adversarial tests

- Mutation test using a single-quoted colon-bearing filename in `EndToEndHostileVaultFilenameTest`.
- Preferably, an AST-based assertion proving `self.hostile_name` is assigned `HOSTILE_VAULT_FILENAME`, avoiding quote-style and formatting blind spots.
- The filed `recallweave-jsw` follow-ups remain appropriate and non-blocking:

  - LF/CRLF deterministic-output matrix.
  - Generalized fixture-portability checks.
  - Case-insensitive filesystem behavior.
  - Actual Windows junction refusal.
  - Viewer static-asset validation on Windows.

## 3. Positive findings

- The supplied [.codex-reviews/suite-20260823T012212Z.txt](/Users/josh/particle-workers/recallweave/.codex-reviews/suite-20260823T012212Z.txt) records:

  - 434 tests passing normally.
  - 434 tests passing with `ResourceWarning` promoted to an error.
  - Clean `compileall`.

- The working tree is clean.
- `git diff --check 1d90cb7..HEAD` is clean.
- `git diff 1d90cb7..HEAD -- src` is empty. The no-`src/`-change reading still holds.
- Merge commit `f7c0804` brought in only `CHANGELOG.md`, `viewer/package.json`, and `viewer/package-lock.json`.
- The current hostile filename is portable and retains active Markdown link/image syntax for a meaningful inertness test.
- The AST end-to-end test now uses the shared constant in the current tree.
- No open bead is labeled `blocker` or `needs-human`.
- No prior cycle recorded a Critical finding.

All historical High findings from cycles 1–31 remain closed:

- Inline, block, multiline-citation, bare-CR, indented-structure, and handling-statement Markdown injection.
- Character-budget overflow.
- Falsy-citation and schema-identity compatibility regressions.
- Evidence-boundary collisions and projected-field injectivity failures.
- Missing connection-evidence projection.
- Vacuous or incomplete well-formedness.
- Structural-absence forgery.
- Excluded-content leakage and error-route path disclosure.
- Unverified or uninventoried citations.
- Fabricated passages behind genuine coordinates.
- Incomplete evidence-side shapes and truncation claims.
- Fabricated discovery evidence.
- Unauthenticated persisted edges.
- Fabricated authored links and lost parser state.
- Unbound heading coordinates, levels, and source lines.
- Operator-authored statements mislabeled as cited passages.

## 4. Ranked recommended fixes

1. Replace the regex source scan with a Python AST check that verifies the assignment to `self.hostile_name` directly.
2. Complete the platform-dependent coverage tracked by `recallweave-jsw`, in its documented order.

## 5. Push safety

The tree is safe to push and safe to merge into protected `main`.

The remaining Low finding concerns future regression detection, not current runtime behavior, security, privacy, evidence integrity, compatibility, or reproducibility. It does not block the milestone merge.