VERDICT: FAIL

## 1. Findings

- **High — Exporter emits unsupported shared-tag evidence.**  
  [src/recallweave/viewer.py:149](/Users/josh/particle-workers/recallweave/src/recallweave/viewer.py:149) falls back to producer-claimed `shared_tags` whenever the computed endpoint-tag intersection is empty. Consequently, a candidate whose endpoints share no tags can still be exported with `signals.shared_tags`, falsely presenting a supporting signal.

  Reproduction on the current tree:

  ```text
  {"signals":{"lexical_terms":["real"],"shared_tags":["fabricated"]}, ...}
  ```

  This was produced by calling `_edge_evidence(..., shared_tags=[])` with persisted `shared_tags: ["fabricated"]`.

  The problem reaches visible UI: [GraphExplorer.tsx:88](/Users/josh/particle-workers/recallweave/viewer/app/components/GraphExplorer.tsx:88) renders raw normalized signals as `Shared tags: ...` without revalidating them. Although Cold Trails scoring separately validates tags against both endpoints, the ordinary graph explanation does not. This violates the evidence-integrity boundary and the remediation’s stated goal of deriving the signal from the actual endpoint intersection.

- **Low — Base-to-HEAD whitespace validation is not clean.**  
  `git diff --check 2c67665..HEAD` reports trailing whitespace in five archived review-file lines. These are documentation artifacts rather than runtime defects.

## 2. Missing adversarial tests

- Construct an exporter candidate whose persisted evidence claims `shared_tags: ["fabricated"]` while its endpoint `tags_json` values have an empty intersection; assert `signals.shared_tags` is absent.
- Construct conflicting persisted and computed tag lists; assert only the deterministic endpoint intersection is exported.
- Assert GraphExplorer never displays a shared-tag explanation unless the tag exists on both normalized endpoint nodes.
- Cover sorting, deduplication, and the 12-tag exporter cap with more than 12 intersecting tags.

## 3. Positive findings

- The cycle-32b remediation is correctly applied: [rendered-html.test.mjs:159](/Users/josh/particle-workers/recallweave/viewer/tests/rendered-html.test.mjs:159) reads `AtlasProvenanceChrome.ts` and asserts `Index claims:` there.
- Exact SSR coverage verifies valid and conflicting provenance, empty v1/v2 output, hostile HTML escaping, privacy-conflict rendering, and the normalized file-load integration path.
- The supplied harness reports 514 Python tests passing with one skip, the `ResourceWarning` run passing, `compileall` passing, and the complete viewer build/test suite passing.
- File-load rejection retains the prior graph and provenance chrome.
- Cold Trails revalidates shared-tag claims against both endpoint nodes before scoring them.
- Direct Obsidian navigation has been removed pending redesign.
- No reproducibility, path-disclosure, destination-protocol, or excluded-content regression was identified.

## 4. Ranked recommended fixes

1. In `_edge_evidence`, distinguish “computed intersection supplied but empty” from “no computed value supplied.” When `shared_tags` is supplied by `build_viewer_document`, export exactly that intersection—even when empty—and never fall back to persisted claims.
2. Add the mismatched-claim exporter regression test described above.
3. Revalidate shared tags before GraphExplorer renders them, providing defense in depth for imported untrusted graph files.
4. Remove trailing whitespace from the archived review records.

## 5. Push safety

The tree is **not safe to push** because it contains an unresolved evidence-integrity defect.