VERDICT: FAIL

## 1. Findings

### High — Future timestamps create a false Parallel invention claim

[daysSince()](/Users/josh/particle-workers/recallweave/viewer/app/cold-trails.ts:80) clamps every future timestamp to zero. [classifyCandidateEdge()](/Users/josh/particle-workers/recallweave/viewer/app/cold-trails.ts:547) then compares those clamped ages rather than the timestamps themselves.

I reproduced this with:

- graph `generated_at`: `2026-08-28`
- source `created_at`: `2030-01-01`
- target `created_at`: `2040-01-01`

`buildColdTrails()` returned `status: "ok"` and emitted:

```text
Observed created_at 2030-01-01T00:00:00Z and 2040-01-01T00:00:00Z within 14 days across domains.
```

The dates are approximately ten years apart. This is an evidence-integrity violation: the viewer labels a demonstrably false statement as a structural fact. Under the supplied verdict guidance, it blocks passage.

### Medium — Timestamp validation accepts malformed input and silently changes its meaning

[safeIsoTimestamp()](/Users/josh/particle-workers/recallweave/viewer/app/graph-data.ts:155) contains a tautological whitespace check:

```ts
cleaned !== value.trim()
```

It also relies on JavaScript’s normalizing `Date.parse()` behavior without validating calendar components.

Reproduction:

```text
" 2026-01-01T00:00:00Z " => 2026-01-01T00:00:00Z
"2026-02-30T00:00:00Z"   => 2026-03-02T00:00:00Z
```

An untrusted graph can therefore supply an impossible observed date and have Atlas silently convert it into a different date before Dormant, Drift, or Parallel-invention classification.

### Low — Cold Trails documentation contradicts its own trail count

[docs/cold-trails.md](/Users/josh/particle-workers/recallweave/docs/cold-trails.md:63) says the implementation supports “five types,” but its table now lists eight.

## 2. Missing adversarial tests

- Two future `created_at` values separated by years must not qualify as Parallel invention, regardless of `generated_at`.
- One future and one past timestamp must use their actual absolute difference.
- Reject impossible calendar dates such as February 30 and non-leap-year February 29.
- Define and test whether leading/trailing timestamp whitespace is rejected or canonicalized.
- Test the exact 14-day Parallel-invention boundary and one millisecond beyond it.
- Pass imported graphs through `normalizeGraph()` before temporal classification tests; current feature tests mostly construct already-normalized `GraphDocument` objects.
- Assert the truth of the emitted “within 14 days” statement, not merely the presence of a Parallel-invention trail or a `created_at` string.

## 3. Positive findings

- The supplied harness log is green: 506 Python tests with one skip, the ResourceWarning run, and `compileall`.
- The viewer build and all reported viewer tests passed; the log reports 7 rendered-HTML, 19 graph-data, and 21 Cold Trails tests.
- Candidate citations are now bilateral and matched against node paths.
- Shared-tag and mutual-neighbor claims are revalidated before scoring and display.
- Candidate trails are omitted when normalized privacy state shows no passage text.
- Prior export history is accepted only from recognized viewer schema versions.
- Export-history malformed counters are conflict-marked rather than silently accepted.
- Selection is deterministic for the tested valid reference clock and observes the tested node, type, and domain-touch limits.
- No tracked source edits were made during this review.

## 4. Ranked recommended fixes

1. Determine Parallel-invention eligibility from the absolute difference between the two parsed `created_at` instants. Do not derive that difference from clamped ages.
2. Make `safeIsoTimestamp()` validate calendar components strictly and either reject surrounding whitespace or document canonicalization.
3. Add mutation-backed tests for future timestamps, invalid calendar dates, and the exact 14-day boundary.
4. Correct the documented trail count.

## 5. Push safety

The tree is **not safe to push**. The unresolved High evidence-integrity defect fails the review gate.

The checkout is also not clean: `.beads/issues.jsonl` is modified, and three review artifacts plus `uv.lock` are untracked. Repository policy independently prohibits pushing that state.