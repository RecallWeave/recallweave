VERDICT: FAIL

## 1. Findings

### High — Drift truncates microseconds and emits a false structural claim

[viewer/app/cold-trails.ts:784](/Users/josh/particle-workers/recallweave/viewer/app/cold-trails.ts:784) converts `created_at` and `modified_at` to JavaScript `Date` objects, losing sub-millisecond precision. Lines 787–788 then use that truncated difference for the 90-day Drift threshold; lines 791–807 repeat it and render the result as an observed fact.

Reproduction:

```text
created_at:  2026-01-01T00:00:00.000001Z
modified_at: 2026-04-01T00:00:00.000000Z

exactDays:              89.99999999998843
dateBasedDays:          90
qualifiesInCurrentCode: true
shouldQualify:          false
```

The current implementation consequently creates a Drift trail and can state that the timestamps are “90 days apart,” although the exact interval is one microsecond short of the documented threshold.

This is the same precision boundary previously fixed for Parallel invention, but Drift still routes through millisecond-resolution `Date`. It is an evidence-integrity defect because a timestamp claim that does not satisfy the rule is presented as an observed structural fact.

The same conversion at lines 725–729 can incorrectly suppress a Dormant trail when the created-to-modified interval is 90 days minus a sub-millisecond fraction.

No additional Critical, Medium, or Low findings were identified.

## 2. Missing adversarial tests

- Drift must reject an interval of exactly `90 days - 1 microsecond`.
- Drift must accept exactly `90 days` and `90 days + 1 microsecond`.
- Dormant/Drift disambiguation must preserve microsecond precision around the 90-day boundary.
- An end-to-end Python exporter → `normalizeGraph()` → `buildColdTrails()` test must retain microseconds at these boundaries.
- A mutation replacing precision-aware arithmetic with `Date.getTime()` should fail the suite.

## 3. Positive findings

- The supplied [harness results](/Users/josh/particle-workers/recallweave/.codex-reviews/suite-20260828T113329Z.txt) are green: 509 Python tests with one skip, the ResourceWarning-strict run, `compileall`, and all 56 viewer tests. Per the brief, I did not rerun them.
- Parallel-invention comparison now preserves microsecond precision.
- Invalid offsets and years affected by `Date.UTC`’s 0–99 remapping are rejected.
- Python export preserves fractional timestamps while normalizing offsets to UTC.
- `ageBonus` is explicit in the score breakdown and the test oracle verifies the exact formula.
- Conflicted export history does not affect Drift scoring or rendered facts.
- Tours require a Bridge or Island opening.
- Show-another excludes the current tour, and keyboard shortcuts avoid typing fields and activated controls.
- `git diff --check 43730aa..HEAD` passed.
- The tracked working tree is clean and matches `origin/foundry/steward`.

## 4. Ranked recommended fixes

1. Calculate Drift spans using signed `bigint` microsecond arithmetic from `utcEpochMicros`, not JavaScript `Date`.
2. Use the same precise span calculation for Dormant/Drift disambiguation and rendered day counts.
3. Add the exact boundary and end-to-end tests above.

## 5. Push safety

The tree is **not safe to push, promote, or merge**. The unresolved High evidence-integrity defect requires remediation and another adversarial review.