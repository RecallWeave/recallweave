VERDICT: PASS WITH FIXES

## 1. Findings

### Medium — Required structural opening is silently replaced by candidate trails

The selection contract requires at least one Bridge or Island, starting the tour with that structural trail, and says to shorten rather than relax constraints ([docs/cold-trails.md:152](/Users/josh/particle-workers/recallweave/docs/cold-trails.md:152)).

However, `pickTrail()` falls back to the unrestricted candidate list when the phase-specific list is empty ([viewer/app/cold-trails.ts:728](/Users/josh/particle-workers/recallweave/viewer/app/cold-trails.ts:728)). Consequently, the first structural phase at [viewer/app/cold-trails.ts:751](/Users/josh/particle-workers/recallweave/viewer/app/cold-trails.ts:751) selects an ordinary candidate when no Bridge or Island exists.

Current-tree reproduction using four valid, disjoint, bilaterally cited candidate edges and no structural trails:

```json
{"count":2,"types":["unwritten_link","unwritten_link"],"trust":["candidate","candidate"]}
```

The result is reported as successful even though it contains no structural trail and opens with an inferred candidate. This breaks the published evidence-ordering and diversification contract.

### Medium — Same-domain trails are double-counted against the domain limit

The contract permits at most two trails touching one domain ([docs/cold-trails.md:158](/Users/josh/particle-workers/recallweave/docs/cold-trails.md:158)). `endpointDomainsForTrail()` returns one entry per endpoint ([viewer/app/cold-trails.ts:584](/Users/josh/particle-workers/recallweave/viewer/app/cold-trails.ts:584)), and `applyTrailSelection()` increments the counter for every entry ([viewer/app/cold-trails.ts:678](/Users/josh/particle-workers/recallweave/viewer/app/cold-trails.ts:678)). A same-domain trail therefore increments that domain twice and incorrectly exhausts the two-trail allowance after one trail.

Current-tree reproduction with four otherwise eligible, disjoint, same-domain candidates:

```json
{"count":1,"types":["unwritten_link"],"notice":"Only one domain is present; cross-domain trail types are omitted."}
```

Two such trails should be permitted. The implementation confuses endpoint count with trail-touch count.

### Low — Review-cycle metadata is stale and the push tree is not clean

The supplied brief describes Cycle 26d and a 504-test target, but `HEAD` is `eb11170`, based on merged PR #5 commit `4bd4bcf` and containing subsequent viewer.v2 and Cold Trails work. The supplied harness actually reports 506 Python tests.

The working tree also contains:

```text
?? .codex-reviews/review-20260828T025857Z.md
?? .codex-reviews/review-20260828T030255Z.md
?? .codex-reviews/review-20260828T030535Z.md
?? uv.lock
```

Repository policy requires a clean integration tree before pushing.

## 2. Missing adversarial tests

- Build a graph with eligible candidate trails but no Bridge or Island and assert the engine refuses or returns no candidate-only tour.
- Assert the first selected trail is always Bridge or Island.
- Assert absence of a required phase shortens the tour instead of invoking `pickTrail()`’s unrestricted fallback.
- Select two disjoint trails whose endpoints share one domain and assert both remain eligible.
- Assert a third trail touching that domain is excluded.
- Test domain accounting with same-domain and cross-domain trails together.
- Assert every successful tour satisfies all final invariants collectively: structural opening, type/domain limits, unique nodes, sequencing, and three-domain coverage where feasible.
- Add mutation tests that fail if phase fallback or domain deduplication is removed.

## 3. Positive findings

- The supplied harness report records 506 Python tests passing with one skip, both normally and with `ResourceWarning` promoted to an error.
- `python3 -m compileall -q src` passed.
- The viewer production build passed; all 36 reported JavaScript tests passed.
- Cycle-26d’s scanner remediations remain present and covered: defaults/decorators, match subjects/guards, file-handle receiver paths, loop `else`, and independent `AugAssign`.
- Export-history reconciliation now correctly checks first and subsequent export arithmetic.
- Score totals are calculated from the documented weighted components.
- Candidate evidence requires bilateral, endpoint-matching citations.
- The passage bonus requires passage evidence on both endpoints.
- Redundant surprise terms are penalized during rescoring.
- Path-like vault labels are rejected at the Python export boundary.
- Candidate evidence remains explicitly labeled as non-factual.
- Saved Markdown neutralizes the tested untrusted-title and structural-fact injection forms.
- Atlas recomputes privacy classifications from normalized content.
- `git diff --check 4bd4bcf..HEAD` reports no whitespace errors.

## 4. Ranked recommended fixes

1. Remove unrestricted phase fallback: required phases must either select a matching trail or leave that slot empty. Enforce the structural-opening invariant before returning success.
2. Deduplicate endpoint domains before checking and incrementing `domainTouchCounts`.
3. Add end-to-end invariant tests over the final selected tour, rather than testing individual score fields or the presence of trail types.
4. Refresh the cycle metadata and remove or deliberately commit the untracked files.

## 5. Push safety

The tree is **not safe to push**. The runtime issues are Medium rather than Critical/High, but the Cold Trails selection contract remains incorrect and the integration working tree is not clean.