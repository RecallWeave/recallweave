VERDICT: FAIL

## 1. Findings

### High — Large exclusion sets exceed SQLite’s parameter limit and abort contract export

Code regions:

- `src/recallweave/contract.py:777-803`
- `src/recallweave/contract.py:1081-1095`
- `src/recallweave/query.py:124-166`

The z1a implementation expands every excluded note ID twice into SQL placeholders. Both `_excluded_edge_counts()` and `_edge_rows()` can therefore exceed `SQLITE_LIMIT_VARIABLE_NUMBER`.

Concrete reproduction using an in-memory database on the current tree:

```text
variable_limit 250000
excluded_ids 125001
sqlite3.OperationalError: too many SQL variables
```

On this build, `_edge_rows()` fails when `2 * excluded IDs + seed parameters + limit` exceeds 250,000. SQLite builds with lower parameter ceilings fail at substantially smaller vault sizes.

This is a remediation-induced correctness and availability regression: exclusions previously ran per fetched endpoint and did not scale their SQL parameter count with the number of excluded notes. A valid broad tag or glob exclusion can now prevent contract creation altogether.

The current regression test covers only 200 excluded notes and cannot expose the failure.

No new defect was found in the text-item validation tightening.

## 2. Missing adversarial tests

- Set the connection’s `SQLITE_LIMIT_VARIABLE_NUMBER` to a small value with `setlimit()`, create enough excluded notes to cross it, and verify export still succeeds while retaining an allowed lower-ranked edge.
- Exercise the same boundary independently through `_excluded_edge_counts()` and `_edge_rows()`.
- Test a broad tag/glob exclusion over a vault with thousands of excluded notes, not merely 200 edges.
- Verify suppression counts remain exact when excluded edges exceed the 200-row emitted-connection cap.
- The previously recommended mutation tests remain absent:
  - independent sanitizer removal from each emitted document region;
  - independent endpoint-binding bypasses;
  - canonical-form bypasses for each persisted candidate field.

## 3. Positive findings

The supplied harness record `.codex-reviews/suite-20260823T221453Z.txt` reports:

- 463 tests passed, with one platform-appropriate junction skip;
- 463 tests passed with `ResourceWarning` promoted to an error;
- `compileall` passed.

The three class-wide rules still hold on successfully processed inputs:

- Document strings continue through sanitization before emission.
- Source and target evidence remain bound independently to their respective endpoint IDs; the citation cache is keyed by endpoint and citation.
- Candidate `shared_terms`, `method`, and `explanation` are authenticated in their persisted canonical form before sanitized output is trusted.

No historical Critical finding was recorded. The historical High findings summarized by cycle 19 remain closed: no reopened injection, privacy leakage, evidence substitution, canonical-collision, budget, path-disclosure, destination-safety, or deterministic-output defect was found. The new High finding is confined to the z1a exclusion-query strategy.

The post-exclusion cap behavior itself is correct below SQLite’s parameter ceiling, and excluded endpoints are removed before their evidence is validated or emitted.

## 4. Ranked recommended fixes

1. Stop materializing the complete excluded-note set twice as SQL parameters. Stream deterministically ordered relevant edges and apply `_note_excluded()` while scanning, retaining the first 200 allowed edges while continuing as needed for exact suppression accounting.
2. Add a deliberately reduced SQLite variable-limit regression test so the failure is inexpensive and platform-independent.
3. Run the existing sanitizer, endpoint-binding, and canonical-field mutation audits.

## 5. Push safety

The tree is **not safe to push** and **not safe to MERGE into protected `main`**.

Besides the unresolved High finding, the working tree is dirty (`.beads/issues.jsonl` and `docs/SESSION-HANDOFF.md`), and `CHECKPOINT_NOT_APPROVED.md` explicitly marks this integration branch as unapproved.