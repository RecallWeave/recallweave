VERDICT: FAIL

## 1. Findings

### High — Candidate-only Island trails are falsely labeled `AUTHORED LINK`

`islandTrails()` assigns `trust: "authored"` to nodes with authored degree **at most one** and at least two candidate edges at [viewer/app/cold-trails.ts:399](/Users/josh/particle-workers/recallweave/viewer/app/cold-trails.ts:399). The UI converts that value directly into `AUTHORED LINK` at [ColdTrailsTour.tsx:310](/Users/josh/particle-workers/recallweave/viewer/app/components/ColdTrailsTour.tsx:310), and the Markdown exporter repeats the label at [cold-trails.ts:89](/Users/josh/particle-workers/recallweave/viewer/app/cold-trails.ts:89).

The existing fixture demonstrates the defect: `leaf.md` has zero verified edges and two candidate edges, yet the test only asserts that it becomes an Island at [cold-trails.test.mjs:185](/Users/josh/particle-workers/recallweave/viewer/tests/cold-trails.test.mjs:185). In the product, that trail is presented as an authored link.

This blurs candidate and verified evidence and violates the repository’s central evidence-integrity contract.

### High — Saved “citation-rich” exports omit every citation

The documented Save contract promises a “citation-rich item” at [docs/cold-trails.md:196](/Users/josh/particle-workers/recallweave/docs/cold-trails.md:196). However, `exportSavedTrailsMarkdown()` emits trust, titles, surprise terms, and structural facts only; it never reads the trail’s edge or emits either endpoint citation at [viewer/app/cold-trails.ts:84](/Users/josh/particle-workers/recallweave/viewer/app/cold-trails.ts:84).

Consequently, even trails that show evidence in the dialog lose their provenance when exported. The resulting artifact cannot substantiate its candidate relationship or reliably reopen the cited source.

### High — Candidate eligibility does not enforce the documented citation boundary

The evidence contract requires a citation matching `path:line` or `path:line-line` at [docs/cold-trails.md:140](/Users/josh/particle-workers/recallweave/docs/cold-trails.md:140). `scoreCandidateTrail()` checks surprise-term count and a numerical evidence score but never requires or validates a citation at [viewer/app/cold-trails.ts:255](/Users/josh/particle-workers/recallweave/viewer/app/cold-trails.ts:255).

The existing fixtures at [cold-trails.test.mjs:283](/Users/josh/particle-workers/recallweave/viewer/tests/cold-trails.test.mjs:283) provide candidate signals without citations and expect an `ok` result. Thus uncited candidates are deliberately admitted despite the stated evidence floor and the architecture’s promise that discovery candidates carry cited passages.

### High — Untrusted graph text is serialized into Markdown without a rendering boundary

`exportSavedTrailsMarkdown()` interpolates vault-derived note titles and user-supplied graph terms directly into Markdown at [viewer/app/cold-trails.ts:84](/Users/josh/particle-workers/recallweave/viewer/app/cold-trails.ts:84). No escaping, fencing, or inert representation protects:

- note titles;
- surprise terms;
- structural facts if a caller supplies a `ColdTrail`.

For example, a title containing a newline followed by `## Verified findings` can create a forged section in the downloaded artifact. Markdown HTML, links, images, and list structure can likewise be injected. Upstream JSON normalization is not a rendering-safety boundary.

This is both generated-artifact injection and evidence-integrity failure.

### Medium — The primary Cold Trails document still says the shipped feature does not exist

[docs/cold-trails.md:3](/Users/josh/particle-workers/recallweave/docs/cold-trails.md:3) calls Cold Trails “design-only” and states it is not implemented, while HEAD wires it into Atlas. The same document describes release prerequisites and behavior that the implementation does not satisfy.

### Medium — The supplied gate does not validate the changed application surface

The supplied harness artifact reports:

```text
Ran 505 tests
OK (skipped=1)
ResourceWarning-strict: OK
compileall: OK
```

The cycle target said 504 tests, but the extra passing Python test is not itself a failure. The important limitation is that the current HEAD adds 743 lines across the TypeScript/React viewer after `origin/foundry/steward`, while the supplied evidence contains only Python tests and `compileall`. It does not report the viewer build, TypeScript checking, linting, or JavaScript tests. Per instruction, I did not rerun any suite.

### Low — Working tree is not push-clean

`git status --porcelain` reports:

```text
 M .beads/interactions.jsonl
 M .beads/issues.jsonl
?? uv.lock
```

The branch is also one commit ahead of `origin/foundry/steward`. Repository policy requires a clean integration tree before pushing.

## 2. Missing adversarial tests

- An Island node with zero verified edges must never render or export as `AUTHORED LINK`.
- An Island node with one verified and multiple candidate edges must use wording that distinguishes the structural fact from an authored relationship.
- Candidate trails without bilateral valid citations must be refused or explicitly degraded according to an approved contract.
- Malformed citations, absolute paths, missing line numbers, and citations pointing at the wrong endpoint.
- A saved candidate export must contain both endpoint citations and preserve their candidate classification.
- Titles and terms containing newlines, headings, fenced blocks, HTML, links, images, blockquotes, and list markers.
- Byte-for-byte deterministic Markdown export.
- DOM-level assertions for trust labels, citation availability, focus restoration, keyboard shortcuts, dismissal, and “show another.”
- Viewer build, lint, typecheck, and JavaScript test results in the review harness.

## 3. Positive findings

- The supplied Python harness records 505 passing tests with one skip.
- The ResourceWarning-strict Python run also passes.
- `compileall` succeeds.
- Candidate cards produced through the normal candidate path retain the visible `CANDIDATE - NOT A FACT` caveat.
- React text rendering escapes graph text in the live DOM; the injection defect is specifically in the downloaded Markdown serializer.
- Cold Trails selection remains browser-local and no new graph upload or telemetry route was found.
- Candidate ordering is deterministic for equal input order and feedback state.
- `git diff --check 4bd4bcf..HEAD` reports no whitespace errors.

## 4. Ranked recommended fixes

1. Replace the binary trail trust model with labels that accurately distinguish authored edges, candidate relationships, and topology-only structural observations.
2. Require and validate the approved citation evidence before admitting candidate trails.
3. Include bilateral citations in saved items and Markdown exports.
4. Route every Markdown scalar through an inert, tested rendering boundary.
5. Add mutation-resistant tests for each evidence-class and citation rule.
6. Run and capture the viewer build, lint, typecheck, and JavaScript suites.
7. Update `docs/cold-trails.md` to describe the actual shipped behavior and reconcile the dirty working tree.

## 5. Push safety

The tree is **not safe to push**. It contains multiple unresolved High evidence-integrity and artifact-injection defects, lacks test evidence for the changed viewer surface, and is not clean.