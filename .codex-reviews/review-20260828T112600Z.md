VERDICT: FAIL

## 1. Findings

### High — Invalid timestamps are normalized into fabricated UTC facts

[viewer/app/graph-data.ts](/Users/josh/particle-workers/recallweave/viewer/app/graph-data.ts:152) accepts timezone offsets without validating their hour/minute ranges. It also passes years `0000`–`0099` through `Date.UTC`, whose legacy handling maps them into 1900–1999.

Reproduction on the current tree:

```text
2026-01-01T00:00:00+99:99 => 2025-12-27T19:21:00Z
0099-01-01T00:00:00Z      => 1999-01-01T00:00:00Z
0000-01-01T00:00:00Z      => 1900-01-01T00:00:00Z
2026-01-01T00:00:00+24:00 => 2025-12-31T00:00:00Z
```

These values enter normalized node timestamps at lines 367–368 and graph `generated_at` at line 493. Cold Trails then uses them as validated clocks for Dormant, Parallel invention, and Drift eligibility and emits them as “Observed” structural facts in [cold-trails.ts](/Users/josh/particle-workers/recallweave/viewer/app/cold-trails.ts:525), lines 703–823.

This crosses the evidence-integrity boundary: untrusted input is not merely displayed—it is transformed into a different timestamp and presented as an observed structural fact. An impossible offset can move a candidate across the 14-, 90-, or 180-day boundary.

Fix by explicitly rejecting invalid offsets and handling years without `Date.UTC`’s 0–99 remapping, followed by round-trip validation.

### Medium — Displayed score breakdown cannot account for the reported total

Candidate scoring adds `ageBonus` outside `weightedTotal` at [cold-trails.ts](/Users/josh/particle-workers/recallweave/viewer/app/cold-trails.ts:483), but `ScoreBreakdown` has no `ageBonus` field. The returned breakdown at lines 516–519 therefore contains a `total` that cannot be derived from its components.

The UI labels this incomplete object as “Score breakdown” and displays its unexplained total at [ColdTrailsTour.tsx](/Users/josh/particle-workers/recallweave/viewer/app/components/ColdTrailsTour.tsx:389).

The test at `viewer/tests/cold-trails.test.mjs:1152` weakens its oracle to accept any unexplained difference from 0 through 0.15. It checks a bound, not whether the actual age formula produced the total. Add `ageBonus` to the breakdown and assert the exact formula.

### Low — Recorded suite targets are stale

[docs/CODEX-REVIEW-PROMPT.md](/Users/josh/particle-workers/recallweave/docs/CODEX-REVIEW-PROMPT.md:102) says 508 Python and 55 viewer tests. The supplied harness report records 509 Python tests and 56 viewer tests. This does not affect runtime behavior but weakens milestone accounting.

## 2. Missing adversarial tests

- Reject offsets `+24:00`, `-24:00`, `+23:60`, and `+99:99`.
- Verify valid boundary offsets such as `+23:59` and `-23:59`, if those are intentionally supported.
- Reject or correctly normalize years `0000` and `0001`–`0099` without century remapping.
- Feed malformed offsets through `normalizeGraph` and assert they cannot produce Dormant, Parallel invention, or Drift trails.
- Assert `scoreBreakdown.total` exactly equals the documented weighted terms plus an explicit age-bonus field.
- Test negative-epoch fractional timestamps if pre-1970 timestamps are supported; bigint remainder formatting is otherwise unverified.
- Add a byte-for-byte determinism assertion for Cold Trails session Markdown containing fractional timestamps and adversarial Unicode.

## 3. Positive findings

- The supplied harness report is green: 509 Python tests passed with one skip, the ResourceWarning run passed, `compileall` passed, and all 56 viewer tests passed.
- The working tree is clean, and `git diff --check 43730aa..HEAD` reports no whitespace errors.
- Fractional seconds through microsecond precision are retained at the reviewed 14-day Parallel invention boundary.
- Timezone-less timestamps and malformed calendar dates covered by the suite are rejected.
- Candidate trails require bilateral citations matching the corresponding node paths.
- Conflicted export-history claims do not influence Drift scoring or rendered facts.
- Bridge selection uses outgoing authored links, Island degree uses authored edges, and tours without a Bridge/Island opening are refused.
- Markdown session export escapes heading/backtick-forging input.
- Viewer normalization reconciles declared privacy metadata against actual included content.
- No direct vault navigation or graph-upload route was found; the browser viewer remains local-file based.

## 4. Ranked recommended fixes

1. Harden `utcEpochMicros`: validate offset hours/minutes and eliminate `Date.UTC`’s year-0–99 remapping.
2. Add end-to-end tests proving malformed timestamps cannot generate timestamp-backed structural claims.
3. Add an explicit `ageBonus` field to `ScoreBreakdown` and restore an exact scoring oracle.
4. Update the cycle’s expected suite counts.

## 5. Push safety

The tree is clean but **not safe to push for promotion or merge**. The timestamp validator creates an unresolved High evidence-integrity defect.