VERDICT: FAIL

## 1. Findings

### High — Reversed timestamps produce a false Drift claim

[viewer/app/cold-trails.ts:90](/Users/josh/particle-workers/recallweave/viewer/app/cold-trails.ts:90) computes `daysBetween` using an absolute difference. [driftTrails at line 780](/Users/josh/particle-workers/recallweave/viewer/app/cold-trails.ts:780) therefore treats `modified_at` occurring before `created_at` as positive drift.

This contradicts the contract requiring `modified_at` to be at least 90 days after `created_at` in [docs/cold-trails.md:70](/Users/josh/particle-workers/recallweave/docs/cold-trails.md:70). It emits an asserted structural trail from invalid temporal ordering, violating evidence integrity.

Read-only reproduction:

```text
created_at:  2026-06-01T00:00:00Z
modified_at: 2024-01-01T00:00:00Z
```

Actual output:

```text
type: drift
headline: This note's content window drifted far from its creation date.
fact: Observed created_at 2026-06-01T00:00:00Z and modified_at
      2024-01-01T00:00:00Z (882 days apart).
```

The same absolute calculation at [line 725](/Users/josh/particle-workers/recallweave/viewer/app/cold-trails.ts:725) also wrongly excludes such a node from Dormant consideration as though it were valid Drift.

No additional findings were identified.

## 2. Missing adversarial tests

- A node with `modified_at` at least 90 days before `created_at` must not qualify as Drift.
- Reversed timestamps must not trigger Drift’s exclusion from Dormant.
- Directional Drift boundaries should cover exactly 90 days, one microsecond below, and one microsecond above.
- Preserve a separate absolute-gap test for Parallel invention, where endpoint ordering legitimately should not matter.

## 3. Positive findings

- The recorded harness suite passed: 509 Python tests with 1 skip, the ResourceWarning-clean repeat, `compileall`, and all 56 viewer tests.
- Fractional timestamps survive exporter and viewer normalization through microsecond precision.
- Invalid calendar dates, timezone-less timestamps, invalid offsets, and problematic years are rejected.
- Candidate score breakdowns now expose `ageBonus`, and reported totals match the implemented formula.
- Conflicted export history is excluded from Drift scoring and displayed facts.
- Cold Trails requires a Bridge or Island opening and the keyboard handlers guard interactive controls.
- The working tree is clean and matches `origin/foundry/steward`.

## 4. Ranked recommended fixes

1. Introduce a directional `daysAfter(created_at, modified_at)` calculation for Drift. Reject negative spans.
2. Use that directional result for both Drift eligibility and Dormant’s Drift exclusion; retain absolute difference only for Parallel invention.
3. Add the reversed-order and microsecond boundary tests listed above.

## 5. Push safety

The tree is **not safe to push, promote, or merge**. The unresolved High evidence-integrity defect requires remediation and another adversarial review.