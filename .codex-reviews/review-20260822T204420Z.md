VERDICT: FAIL

## 1. Findings

### High — public builder emits evidence that its validator declares malformed

[src/recallweave/contract.py:145](/Users/josh/particle-workers/recallweave/src/recallweave/contract.py:145) preserves any whitelisted evidence-side leaves independently. Consequently, input such as:

```json
{
  "source_evidence": {"citation": "Alpha.md:1-2"},
  "shared_terms": ["alpha"]
}
```

produces a citation-only `source_evidence`.

The new predicate correctly rejects this at [src/recallweave/contract.py:116](/Users/josh/particle-workers/recallweave/src/recallweave/contract.py:116), because the side lacks `passage`. However, `build_contract_document()` never calls that predicate: it reads the edge JSON at [src/recallweave/contract.py:435](/Users/josh/particle-workers/recallweave/src/recallweave/contract.py:435) and unconditionally appends the resulting evidence at [src/recallweave/contract.py:444](/Users/josh/particle-workers/recallweave/src/recallweave/contract.py:444).

Concrete failure scenario:

1. An existing schema-v2 index contains a candidate edge with a citation-only, heading-only, or `truncated`-only evidence side.
2. The public `recallweave contract` command reads that index.
3. It emits a canonical JSON contract for which `connection_evidence_is_well_formed(connection)` is false.
4. Markdown then renders the partial side, using structural absence markers for its missing leaves.

This means the advertised “reject path” is only a predicate used by tests; malformed source evidence is not rejected by the public builder. It also contradicts the claim at [docs/task-contracts.md:403](/Users/josh/particle-workers/recallweave/docs/task-contracts.md:403) that the builder always emits every applicable projected key.

The existing positive builder test at [tests/test_contract_document.py:218](/Users/josh/particle-workers/recallweave/tests/test_contract_document.py:218) only exercises freshly generated, known-good evidence. The malformed-side tests at [tests/test_contract_document.py:376](/Users/josh/particle-workers/recallweave/tests/test_contract_document.py:376) call the predicate directly and therefore do not establish public-path rejection.

This is an evidence-integrity defect and requires `FAIL`.

### Medium — documented omitted-field inventory is incomplete

[docs/task-contracts.md:369](/Users/josh/particle-workers/recallweave/docs/task-contracts.md:369) says the following list contains the canonical JSON fields omitted from Markdown, but it lists only ten `retrieved_context` fields.

At minimum, these canonical connection-evidence fields are also omitted:

- `connections[].evidence.source_evidence.truncated`
- `connections[].evidence.target_evidence.truncated`
- `connections[].evidence.method`
- `connections[].evidence.explanation`

The document acknowledges the first category elsewhere at [docs/task-contracts.md:169](/Users/josh/particle-workers/recallweave/docs/task-contracts.md:169), making the inventory internally inconsistent.

The strengthened omission test does not catch this drift because `_not_projected_path()` explicitly supports only `retrieved_context[]` at [tests/test_contract_projection.py:819](/Users/josh/particle-workers/recallweave/tests/test_contract_projection.py:819). Thus the value-invariance proof does not cover all fields the implementation omits.

## 2. Missing adversarial tests

- Mutate an indexed candidate edge’s `evidence_json` to contain citation-only, heading-only, empty, and `truncated`-only sides; invoke `build_contract_document()` and assert the documented rejection behavior.
- Assert every connection returned by the public builder satisfies `connection_evidence_is_well_formed()`, using malformed persisted edge JSON rather than only a freshly built index.
- Test the chosen behavior when one edge is malformed among several: reject the entire export or discard that edge explicitly and account for it. The contract currently specifies neither.
- Extend value-invariance coverage to omitted connection fields: both side-level `truncated`, `method`, and `explanation`.
- Add a complete canonical-schema-to-projected/omitted partition test so every canonical leaf belongs to exactly one documented set.
- Exercise malformed evidence whose cost is zero, such as citation-only or `truncated`-only sides, to ensure budget admission cannot bypass validation.

## 3. Positive findings

- The supplied harness result reports 372 tests passing and `compileall: OK`.
- The working tree was clean, and `HEAD` matched `origin/foundry/task-contracts`.
- The new `_field()` behavior genuinely separates ordinary absent fields structurally: absent values produce trusted label-plus-marker chrome, while present values—including the literal `None recorded.`—produce fenced blocks.
- The `verified` special case correctly passes `None` into `_field()` instead of substituting the marker string.
- Vault/operator-derived values rendered through ordinary projected fields remain fenced; labels and the empty-section marker are renderer-authored.
- `exclusions.enforced` cannot be forged by arbitrary string content through the public builder because the builder emits the trusted boolean `True`.
- The evidence applicability predicate itself rejects partial sides, unknown leaves, invalid leaf types, invalid top-level members, and missing required `shared_terms`.
- Projection-order tests now compare rendered element order and preserve multiplicity instead of merely checking membership.
- The retrieved-context omission test uses value invariance and is materially stronger than its formatting-dependent predecessor.

## 4. Ranked recommended fixes

1. Enforce `connection_evidence_is_well_formed()` inside `build_contract_document()` before admitting each connection. Define and document whether malformed persisted evidence aborts the export or suppresses the connection; silent partial projection is unacceptable.
2. Add public-builder regression tests using deliberately malformed `edges.evidence_json`.
3. Make the documented projected and omitted sets an exhaustive partition of canonical contract fields.
4. Generalize the omission invariance test beyond `retrieved_context[]` to connection evidence and all other intentionally omitted fields.

## 5. Push safety

The tree is **not safe to push as an approved or promotable revision**. The checkpoint is clean and its supplied suite is green, but the unresolved High evidence-integrity defect blocks promotion, merge, release, deployment, and milestone PR progression.