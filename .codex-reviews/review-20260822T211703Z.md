VERDICT: PASS WITH FIXES

## 1. Findings

### Low — tests still leak a SQLite connection

Several tests use `with sqlite3.connect(...)`, which commits or rolls back but does not close the connection:

- `tests/test_contract_cli.py:224`
- `tests/test_contract_document.py:942`
- `tests/test_contract_document.py:1042`
- `tests/test_contract_document.py:1098`
- `tests/test_contract_document.py:1179`
- `tests/test_contract_evidence_bounds.py:121`

The supplied suite output confirms the leak:

```text
ResourceWarning: unclosed database in <sqlite3.Connection object ...>
Ran 382 tests in 11.029s
OK
compileall: OK
```

The warning happened to surface inside `mistletoe`, but that is merely where garbage collection occurred. This is test-resource hygiene rather than a product defect. It also slightly undermines the cycle note’s claim that the harness cleanup is now deterministic.

Use `contextlib.closing(sqlite3.connect(...))`, or explicitly close each connection.

No Critical, High, or Medium findings remain.

## 2. Missing adversarial tests

- Run the suite with `ResourceWarning` promoted to an error, outside the CLI helper’s intentional warning-suppression window. This would catch leaked test connections deterministically.
- Corrupt multiple edges so an earlier valid-but-over-budget edge precedes a later malformed edge. Pin whether validation is intentionally limited to edges reached before budget truncation.
- Exercise malformed evidence whose edge ID has more than one digit, ensuring the diagnostic remains content-free without tests accidentally depending on a single-digit fixture ID.
- Add a builder-driven probe for every supported scalar collection with both empty and populated forms, rather than relying on the aggregate five-shape corpus to happen to populate them.

## 3. Positive findings

- The supplied gate run passed all 382 tests; `compileall` also passed.
- The malformed-evidence diagnostic now emits only the database edge ID and evidence class, not endpoint paths.
- Excluded endpoints are suppressed before malformed-evidence validation.
- The edge ID is a SQLite integer primary key and is locally resolvable; it does not directly contain note paths or text.
- The renderer continues to fence untrusted values and keeps one field per fence.
- Projected and omitted fields are disjoint and tested against multiple public builder shapes.
- Omitted-field probes now vary falsiness and cardinality, detecting truthiness, length, empty-string, and zero-value reads.
- No runtime dependency was added; `mistletoe` remains test-only.
- The working tree is clean, and `HEAD` matches `origin/foundry/task-contracts`.

## 4. Ranked recommended fixes

1. Close all directly opened SQLite test connections deterministically.
2. Add a warnings-as-errors test invocation to CI.
3. Add the later-malformed-edge/budget-ordering test to make the validation boundary explicit.

## 5. Push safety

The tree is safe to push as a checkpoint. The remaining issue is Low severity and confined to test cleanup; it does not justify blocking the implementation checkpoint. Promotion, merge, or release should wait until the SQLite warning is removed and the suite runs warning-clean.