VERDICT: FAIL

## 1. Findings

- **High — required nullable `previous_content_hash` can be omitted while the predecessor is accepted.** In [`src/recallweave/viewer.py:201`](/Users/josh/particle-workers/recallweave/src/recallweave/viewer.py:201), `value.get("previous_content_hash")` makes an absent key indistinguishable from explicit JSON `null`. This violates the required frozen `export_history` shape in [`docs/json-output.md:347`](/Users/josh/particle-workers/recallweave/docs/json-output.md:347) and the cycle-29c requirement for a complete v2 contract.

  Reproduction on the current tree:

  ```text
  missing_previous_content_hash_validated= True
  ```

  A hand-crafted v2 predecessor containing nodes, edges, and all other history fields—but omitting `previous_content_hash`—therefore qualifies as a prior approved export. A subsequent forced export derives a predecessor digest and change counters from an incomplete document. This is the same evidence-integrity boundary cycle 29c intended to close.

## 2. Missing adversarial tests

- A v2 predecessor whose `export_history` omits `previous_content_hash` must be rejected.
- Independently remove each required `export_history` key and assert rejection, including the nullable field.
- Add an end-to-end forced-replacement test confirming an incomplete history produces `previous_content_hash: null`, rather than only testing the helper.
- Mutation-test replacing explicit key-presence validation with `.get(...)`; the new test must kill that mutation.
- Test malformed `export_id` values containing controls or surrounding whitespace, establishing whether the producer contract requires rejection or normalization.

## 3. Positive findings

- The harness reports 511 Python tests passing with one skip, including the ResourceWarning run; `compileall` passed.
- Viewer build and all viewer tests passed: 7 rendered-HTML, 21 graph-data, and 30 Cold Trails tests.
- Missing or incorrectly typed `edges` is now rejected for both empty and non-empty predecessors.
- A missing whole v2 `export_history` object is rejected.
- Required history counters are checked as non-negative integers, with booleans rejected.
- The accepted empty-prior fixture is producer-shaped and yields the deterministic empty SHA-256 digest.
- Parallel invention no longer suppresses simultaneous Unwritten/Distant classification.
- Reversed Drift timestamps, citation-backed passage-free candidates, evidence-class separation, and deterministic Cold Trails output remain covered.
- `git diff --check 1fd322a..HEAD` reported no whitespace errors.
- The working tree is clean, and the two previous review artifacts are tracked.
- No files were modified during this review.

## 4. Ranked recommended fixes

1. Require `"previous_content_hash" in value` before validating its nullable value.
2. Add field-removal and end-to-end forced-replacement regression tests.
3. Add mutation coverage for every required `export_history` key.
4. Clarify and test normalization versus rejection for `export_id`.

## 5. Push safety

The tree is **not safe to push**. The suite is green and the tree is clean, but an unresolved High evidence-integrity defect remains.