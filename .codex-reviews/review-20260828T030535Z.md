VERDICT: FAIL

## 1. Findings

### High — Cold Trails selection depends on the browser locale

[viewer/app/cold-trails.ts:183](/Users/josh/particle-workers/recallweave/viewer/app/cold-trails.ts:183) and [viewer/app/cold-trails.ts:207](/Users/josh/particle-workers/recallweave/viewer/app/cold-trails.ts:207) use `toLocaleLowerCase()` without fixing a locale. This contradicts the byte-stability contract that the same graph and feedback produce identical scoring and selection at [docs/cold-trails.md:99](/Users/josh/particle-workers/recallweave/docs/cold-trails.md:99).

A fixture using title `TITLE` and candidate terms `["title", "novel"]` produced different eligibility when the default-locale operation was exercised as English versus Turkish:

```text
{"locale":"en-US","status":"refused","count":0,"reason":"no_surprise_terms"}
{"locale":"tr","status":"ok","count":1,"reason":null}
```

In Turkish, uppercase `I` lowercases to dotless `ı`, so `title` is no longer recognized as already present in the title and becomes a false “surprise” term. The same graph can therefore be refused in one browser and presented as surprising in another.

### Medium — Evidence documentation still describes the superseded one-sided boundary

The remediated implementation correctly requires source and target citations at [viewer/app/cold-trails.ts:146](/Users/josh/particle-workers/recallweave/viewer/app/cold-trails.ts:146). The documented evidence floor still requires only “a cited source passage” and singular “citation” at [docs/cold-trails.md:115](/Users/josh/particle-workers/recallweave/docs/cold-trails.md:115) and [docs/cold-trails.md:140](/Users/josh/particle-workers/recallweave/docs/cold-trails.md:140).

That leaves the shipped security/evidence contract weaker than the actual boundary and could authorize a future regression to one-sided evidence.

### Low — Checkpoint marker is stale

[CHECKPOINT_NOT_APPROVED.md:9](/Users/josh/particle-workers/recallweave/CHECKPOINT_NOT_APPROVED.md:9) still names cycle 26c as the latest verdict despite later committed and local review cycles.

### Low — Working tree is not push-clean

`git status --porcelain` reports:

```text
?? .codex-reviews/review-20260828T025857Z.md
?? .codex-reviews/review-20260828T030255Z.md
?? uv.lock
```

Repository policy requires a clean integration tree before pushing.

## 2. Missing adversarial tests

- Run the same Cold Trails fixture under English and Turkish case-folding and assert identical qualification, ordering, scores, and refusal state.
- Include Turkish `I/İ/ı/i` and Greek sigma variants in titles, tags, domains, paths, and evidence terms.
- Assert the documented evidence floor explicitly requires endpoint-matching bilateral citations.
- Mutation tests proving removal of either source- or target-citation validation rejects the remediation.
- A test asserting the checkpoint marker names the newest committed review.

## 3. Positive findings

- The supplied harness report records 505 Python tests passing with one skip, including the ResourceWarning-strict run.
- `compileall` succeeded.
- All 32 reported viewer tests passed, including the production build.
- Cycle-26d’s defaults/decorators, match subject/guard, handle-write, for-else, and independent AugAssign scanner remediations are present and covered.
- Candidate admission now requires both citations and verifies each citation against its corresponding endpoint.
- Source and target passages and citations are rendered separately.
- Saved candidate exports include bilateral citations and escape tested Markdown-forging input.
- Direct vault-navigation links were removed in favor of copying relative paths.
- `git diff --check 374be23..HEAD` reports no whitespace errors.

## 4. Ranked recommended fixes

1. Replace locale-dependent case conversion with deterministic Unicode handling, such as `toLowerCase()` or an explicitly fixed locale, throughout tokenization and surprise-term comparison.
2. Add cross-locale regression and mutation tests.
3. Update the evidence-floor documentation to require valid, endpoint-matching source and target citations.
4. Refresh the checkpoint marker.
5. resolve the three untracked files before pushing.

## 5. Push safety

The tree is **not safe to push**. It has an unresolved High deterministic-output defect and is not clean.