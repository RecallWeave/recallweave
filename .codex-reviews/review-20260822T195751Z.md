VERDICT: FAIL

## 1. Findings

### High — “Well-formed” remains incomplete and permits byte-identical distinct documents

`connection_evidence_is_well_formed()` checks only whether top-level evidence members are present or forbidden. It does not validate the contents of an optional evidence side ([contract.py:65](/Users/josh/particle-workers/recallweave/src/recallweave/contract.py:65)). Meanwhile, the renderer ignores unprojected side members and treats an invalid/non-dict side like an absent side ([contract_markdown.py:218](/Users/josh/particle-workers/recallweave/src/recallweave/contract_markdown.py:218)).

Consequently, these two distinct `discovery_candidate` documents are both classified well-formed and render byte-for-byte identically:

- `evidence={"shared_terms":["x"]}`
- `evidence={"shared_terms":["x"], "source_evidence":{"truncated":true}}`

Reproduction output:

```text
a_well_formed= True
b_well_formed= True
byte_identical= True
b_source= {'truncated': True}
```

This directly contradicts the documented claim that well-formedness requires every applicable projected key and that two well-formed documents differing in projected evidence cannot render identically ([task-contracts.md:379](/Users/josh/particle-workers/recallweave/docs/task-contracts.md:379), [task-contracts.md:416](/Users/josh/particle-workers/recallweave/docs/task-contracts.md:416)).

The builder can produce partial sides because `_edge_evidence()` independently retains any subset of `citation`, `heading`, and `passage`, plus `truncated` ([contract.py:96](/Users/josh/particle-workers/recallweave/src/recallweave/contract.py:96)). Thus this is not merely a renderer concern for arbitrary malformed objects.

This is a reproducibility-contract failure and means the cycle-12 High finding is not fully remediated.

### Medium — The applicability table is not actually the single source of truth

The table governs only the three top-level members named in `_EVIDENCE_MEMBERS`, which is itself unused ([contract.py:38](/Users/josh/particle-workers/recallweave/src/recallweave/contract.py:38)). Nested side-field applicability and types remain implicit in `_edge_evidence()` and the renderer.

The tests enumerate hand-selected top-level shapes ([test_contract_document.py:313](/Users/josh/particle-workers/recallweave/tests/test_contract_document.py:313)) but omit:

- Partial side dictionaries.
- Side dictionaries containing only `truncated`.
- Non-dict optional sides.
- Required `shared_terms` with a non-list or `None` value.
- Unknown evidence members.

Therefore validity is not decidable from the table alone as claimed.

### Medium — Disclosure regression detection still depends on exact presentation

The repaired disclosure test detects most omissions by searching for the current textual serialization, while `retrieved_context[].truncated` is detected only through a narrow label pattern ending in `truncated` ([test_contract_projection.py:1013](/Users/josh/particle-workers/recallweave/tests/test_contract_projection.py:1013)).

A renderer change such as:

```text
Passage 1 was truncated:
```

or a formatted integer such as `1,234,567` would disclose the field while evading the test. The test verifies today’s representation, not the stronger property that the renderer never reads or emits those fields.

## 2. Missing adversarial tests

- Assert that optional evidence sides, when present, are dictionaries with a complete, explicitly defined leaf shape.
- Reject `source_evidence={"truncated": True}` and other sides containing no projected evidence.
- Reject `shared_terms=None`, strings, mappings, and mixed-type lists even though the key is present.
- Compare rendering of absent, `None`, empty, and partial optional evidence sides under the well-formedness predicate.
- Mutation-test every applicability status and every nested leaf, rather than enumerating only selected top-level shapes.
- Test disclosure omission structurally—for example by instrumenting field access or using a mapping that raises when a prohibited key is read—so formatting changes cannot defeat detection.
- Demonstrate that documented applicability prose changes fail against the runtime table; currently only the projected-field list has a direct equality test.

## 3. Positive findings

- The saved harness result reports all 363 tests passing and `compileall` clean.
- The new negative test correctly rejects a `discovery_candidate` missing `shared_terms`.
- Authored links carrying forbidden top-level passage evidence are rejected.
- The restored projected-field equality check directly compares documentation with `PROJECTED_FIELDS` in both drift directions.
- Non-string disclosure sentinels now use type-correct injected values.
- The reviewed working tree is clean.
- No tree modifications were made during review.

## 4. Ranked recommended fixes

1. Expand the applicability schema to define nested side shape and value types, then make `connection_evidence_is_well_formed()` validate them completely.
2. Add the byte-identical reproduction above as a regression test and require that either the second document be rejected or render distinctly.
3. Derive builder validation and tests from the same complete schema; remove or use `_EVIDENCE_MEMBERS` so it cannot drift.
4. Replace substring-based disclosure checks with structural prohibited-field access checks.
5. Mechanically validate the documentation’s evidence-class table against the runtime schema.

## 5. Push safety

The tree is **not safe to push**. The unresolved High reproducibility/well-formedness defect requires `VERDICT: FAIL`.