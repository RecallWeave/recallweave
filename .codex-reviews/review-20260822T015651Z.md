VERDICT: FAIL

## 1. Findings

### High — Markdown injection remains possible through the handling statement

[src/recallweave/contract_markdown.py:115](/Users/josh/particle-workers/recallweave/src/recallweave/contract_markdown.py:115) passes `handling.statement` directly to `_quote_line()` without inline escaping. Blockquote syntax does not neutralize inline Markdown, so images and links remain active.

Reproduction:

```python
document["handling"]["statement"] = (
    "Safe ![beacon](https://attacker.example/pixel)\n## forged"
)
print(render_contract_markdown(document))
```

Current output:

```markdown
> Safe ![beacon](https://attacker.example/pixel)
> ## forged
```

The image is still live Markdown. This contradicts the claimed fix covering every unfenced field and violates the rule that upstream sanitization is not a rendering boundary. Documents produced by the current builder use a constant handling statement, but `render_contract_markdown()` is a public rendering API accepting caller-provided documents; its other fields are already tested under that threat model.

The same routing problem exists for schema-constrained scalar fields at [contract_markdown.py:223](/Users/josh/particle-workers/recallweave/src/recallweave/contract_markdown.py:223) and [contract_markdown.py:248](/Users/josh/particle-workers/recallweave/src/recallweave/contract_markdown.py:248): suppressed counts and budget values use `_as_str()` without escaping. Proper builder output supplies numbers, but malformed or caller-created documents can inject newlines or inline constructs.

### Low — New inline tests do not validate rendered Markdown semantics

[tests/test_contract_markdown_injection.py:267](/Users/josh/particle-workers/recallweave/tests/test_contract_markdown_injection.py:267) primarily checks that selected literal substrings are absent. It does not parse the result with a CommonMark implementation and assert that no link, image, HTML, or unintended structural node exists.

This can miss equivalent constructs, unusual destinations, entity-based forms, and escaping interactions. It also missed `handling.statement` entirely.

### Low — No cumulative cited-passage regression test

[tests/test_contract_document.py:332](/Users/josh/particle-workers/recallweave/tests/test_contract_document.py:332) tests only one cited item. The implementation at [contract.py:233](/Users/josh/particle-workers/recallweave/src/recallweave/contract.py:233) does sum all cited passages, so the cumulative behavior appears correct by inspection, but the specifically requested several-items case is not pinned against regression.

## 2. Missing adversarial tests

- Put live image, link, autolink, emphasis, backticks, and embedded newlines in `handling.statement`.
- Supply hostile string values for `budget.characters_used`, `budget.character_budget`, and each `exclusions.suppressed` field to the public renderer.
- Parse rendered output as CommonMark and assert that hostile unfenced fields produce no image, link, raw-HTML, or unexpected heading/list nodes.
- Exercise pre-escaped backslashes adjacent to every escaped metacharacter.
- Use multiple cited passages that fit individually but cumulatively exceed the budget.
- Combine cited passages consuming most of the budget with retrievable context and connection evidence, verifying exact `characters_used`, connection admission, and `truncated`.
- Verify the documented actionable error message for cited-plus-operator budget exhaustion, not merely the exception type.

## 3. Positive findings

- The supplied harness report records all 279 unit tests passing and `compileall` succeeding.
- The new budget check accounts for cited passages cumulatively before retrieval and connection admission.
- Retrieved context and connection evidence remain subordinate to the remaining budget.
- The new metacharacter escaping covers the principal task, acceptance, cited-item, retrieved citation, connection-table, exclusion, and provenance-citation routes.
- Multiline passage-like content remains fenced with dynamically sized fences rather than being double-escaped.
- The determinism test is no longer tautological and verifies UTC timestamps plus equality after removing `generated_at`.
- No source-tree edits were made during this review.

## 4. Ranked recommended fixes

1. Route every line of `handling.statement` through inline escaping while preserving blockquote structure.
2. Escape or strictly validate suppressed-count and budget scalar values before rendering.
3. Add CommonMark AST-based safety assertions rather than relying solely on substring absence.
4. Add cumulative cited-budget and cited/retrieval/connection interaction tests.

## 5. Push safety

The tree is **not safe to push**. An unresolved High Markdown-rendering injection remains.