VERDICT: PASS WITH FIXES

## 1. Findings

### Medium — Index provenance strings bypass the universal sanitizer

[src/recallweave/contract_provenance.py:7](/Users/josh/particle-workers/recallweave/src/recallweave/contract_provenance.py:7) reads `schema_version` and `indexed_at` directly from the database, and [src/recallweave/contract_provenance.py:21](/Users/josh/particle-workers/recallweave/src/recallweave/contract_provenance.py:21) emits them unchanged.

This contradicts [docs/task-contracts.md:970](/Users/josh/particle-workers/recallweave/docs/task-contracts.md:970), which promises that every string reaching the document passes through the sanitizer. A malformed or edited index can therefore place bidi, zero-width, or control characters in either provenance field, and those bytes reach JSON and Markdown.

This is not presently a privacy leak or evidence-integrity violation: the fields cannot introduce vault passages, alter evidence classification, or escape Markdown fences. It is a completeness gap in the sanitization invariant, analogous to the previously reported endpoint-metadata issue, so I classify it Medium.

I could not mutate the SQLite fixture in the read-only review environment. The source path is direct: `meta.value` → `meta_value()` → `provenance.index.schema_version/indexed_at`.

## 2. Missing adversarial tests

- Modify `meta.schema_version` and `meta.indexed_at` to contain `\u202e`, `\u200b`, and a C0 control; assert none reaches the canonical JSON or Markdown.
- Add a recursive assertion over a maximally populated contract that every emitted string satisfies `sanitize(value) == value`. The existing hostile-text test does not exercise index provenance.
- Mutation-test removal of sanitization independently from retrieved metadata, connection endpoints, evidence leaves, exclusion receipts, and index provenance.
- Preserve the new table-driven path/glob/tag exclusion test; additionally assert the distinctive excluded content is absent from rendered Markdown and provenance citations, not only from `json.dumps(document)`.

## 3. Positive findings

The supplied harness results are valid and clean:

- 456 tests passed normally.
- 456 tests passed with `ResourceWarning` promoted to an error.
- `compileall` completed successfully.
- The working tree is clean and matches `origin/foundry/task-contracts`.

Applying the requested quantification lens:

- `sanitize`: vault-side exclusion matching now covers all three selector classes—exact paths, globs, and tags. Retrieved metadata, connection endpoints, evidence strings, operator fields, and exclusion receipts are sanitized. The only uncovered emitted class I found is the two index-provenance strings above.
- Endpoint binding: both `source_evidence` and `target_evidence` are checked against their respective endpoint IDs. The resolution cache is keyed by `(note_id, citation)`, sanitized citation paths are compared consistently, excluded endpoints are removed before authentication, and accepted connection citations alone reach provenance. I found no unbound member or alternate route to excluded content.
- Canonical-form requirement: all persisted candidate strings subject to normalization collision—`shared_terms`, `method`, and `explanation`—are checked in their raw persisted form before sanitized values are authenticated. Evidence-side citation, heading, and passage strings correctly are not subjected to that same requirement because the indexer can genuinely derive them from filenames and vault text containing characters later removed for emission.

Every Critical and High finding from the recorded cycles remains closed, including:

- Markdown structure and inline injection;
- budget overflow and truncation errors;
- schema/projection loss, field-boundary collisions, and absence forgery;
- malformed or fabricated evidence;
- diagnostic path disclosure;
- citation and passage attribution;
- candidate shared-term fabrication;
- unauthenticated edge classification;
- authored-link parser-state, resolution, and heading-coordinate gaps;
- operator wording misclassified as source evidence;
- duplicate-key exclusion override;
- cross-endpoint exclusion leakage;
- invisible-character path and tag exclusion bypasses.

No unresolved Critical or High finding remains.

## 4. Ranked recommended fixes

1. Sanitize or strictly validate `schema_version` and `indexed_at` inside `index_provenance()`.
2. Add the recursive whole-document sanitizer invariant test using a maximally populated hostile fixture.
3. Extend the new exclusion matrix’s assertions explicitly across JSON, Markdown, connections, and provenance.

## 5. Push safety

Yes. Under the stated verdict policy, this tree is safe to push and safe to merge into protected `main`: no Critical, High, privacy, evidence-integrity, compatibility, reproducibility, or test failure remains.

It is not completely clean; the Medium provenance-sanitization gap should be fixed in the next patch.