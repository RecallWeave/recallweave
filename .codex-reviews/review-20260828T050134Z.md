VERDICT: PASS WITH FIXES

## 1. Findings

### Medium — Viewer v2 documentation still contains a v1 receipt contract

The documentation states that new graph documents and stdout receipts use `recallweave.viewer.v2` at [docs/json-output.md:127](/Users/josh/particle-workers/recallweave/docs/json-output.md:127) and [docs/json-output.md:317](/Users/josh/particle-workers/recallweave/docs/json-output.md:317), matching the implementation.

However, the canonical receipt example still declares:

```json
"schema_version": "recallweave.viewer.v1"
```

at [docs/json-output.md:175](/Users/josh/particle-workers/recallweave/docs/json-output.md:175). The options list at [docs/json-output.md:148](/Users/josh/particle-workers/recallweave/docs/json-output.md:148) also omits the new `--vault-name` option.

A consumer built from the example could reject current receipts or incorrectly treat them as legacy v1. This is a documentation/compatibility-oracle defect; the runtime behavior itself is consistent with the frozen v2 contract.

### Low — Review context and working tree are stale

The brief names Cycle 26d and a 504-test target. The actual checkout is `foundry/steward` at `a3e6770`, 13 commits beyond merged PR #5 base `4bd4bcf`, and contains viewer.v2 and Cold Trails work. The supplied harness ran 506 Python tests.

The working tree is dirty with five untracked files:

```text
.codex-reviews/review-20260828T025857Z.md
.codex-reviews/review-20260828T030255Z.md
.codex-reviews/review-20260828T030535Z.md
.codex-reviews/review-20260828T045931Z.md
uv.lock
```

That violates the repository’s clean-tree checkpoint-push condition.

## 2. Missing adversarial tests

- Assert the documented receipt example’s schema version against a real `export-viewer` receipt.
- Assert the documented option list includes every public `export-viewer` flag, particularly `--vault-name`.
- Exercise three individually eligible domains whose trails cannot all be selected together because of node reuse or domain-touch limits; assert deterministic refusal.
- Exercise the eligible-domain invariant with preexisting feedback that makes the third domain unavailable.
- Add an aggregate oracle over every successful tour: structural first, maximum two trails per type/domain, unique nodes, hub exclusion, three eligible domains when applicable, and final Reinforced ordering.
- Mutation-test removal of the final eligible-domain coverage check.

## 3. Positive findings

- The supplied harness reports 506 Python tests passing with one skip, both normally and with `ResourceWarning` promoted to an error.
- `compileall` passed.
- The viewer production build passed, along with all 39 reported JavaScript tests.
- `git diff --check 4bd4bcf..HEAD` reports no whitespace errors.
- The latest remediation computes the three-domain requirement from individually eligible trails and validates coverage before returning success.
- Candidate admission requires bilateral citations matching the respective endpoint paths.
- Candidate evidence remains labeled `CANDIDATE - NOT A FACT`; structural and authored claims remain distinct.
- Markdown session export escapes untrusted scalar content.
- Atlas recomputes privacy profiles from normalized displayed content and labels provenance metadata as source claims.
- Graph normalization enforces byte/record limits, sanitizes unsafe controls, and drops duplicate or dangling records.
- The cycle-26d scanner remediations remain present for defaults/decorators, match subjects and guards, handle-write receivers, loop `else`, and independent `AugAssign`.

## 4. Ranked recommended fixes

1. Change the receipt example at `docs/json-output.md:177` to `recallweave.viewer.v2`.
2. Document `--vault-name` in the public option list.
3. Add documentation/runtime contract tests for the receipt version and CLI options.
4. Add the mutually constrained three-domain and aggregate selection-oracle tests.
5. Refresh the review-cycle metadata and resolve the five untracked files.

## 5. Push safety

The tree is **not safe to push** under the repository’s checkpoint rules: one Medium documentation-contract defect remains, and the working tree is not clean. No Critical or High runtime, security, privacy, evidence-integrity, or reproducibility defect was found.