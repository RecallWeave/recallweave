VERDICT: FAIL

## 1. Findings

### High — Connection fields still lose their evidence boundary and collide byte-for-byte

[src/recallweave/contract_markdown.py:226](/Users/josh/particle-workers/recallweave/src/recallweave/contract_markdown.py:226) concatenates `source`, `target`, `kind`, and `verified` into one fenced block. This directly contradicts the newly documented one-block-per-field invariant and allows distinct endpoint assignments to produce identical Markdown.

Reproduction on the current tree:

```text
document A:
  source = "A\ntarget: X"
  target = "B"

document B:
  source = "A"
  target = "X\ntarget: B"
```

Both use `kind = "K"` and `verified = true`. Result:

```text
equal: True
Connection 1:
```text
source: A
target: X
target: B
kind: K
verified: true
```
```

A reader cannot determine whether `target: X` belongs to the source value or is renderer-authored attribution. Vault-relative filenames can contain newlines, so this is not confined to malformed synthetic documents.

This is an evidence-integrity failure: materially different connection endpoints render byte-identically. It also contradicts:

- [docs/task-contracts.md:272](/Users/josh/particle-workers/recallweave/docs/task-contracts.md:272), which says two document fields are never placed in one block.
- [docs/task-contracts.md:288](/Users/josh/particle-workers/recallweave/docs/task-contracts.md:288), which promises injectivity.
- [docs/task-contracts.md:295](/Users/josh/particle-workers/recallweave/docs/task-contracts.md:295), which says every field has its own label and fence.
- [CHANGELOG.md:5](/Users/josh/particle-workers/recallweave/CHANGELOG.md:5), which makes the same release claim.

### Medium — The global injectivity claim exceeds what the renderer and tests establish

The documentation promises that any materially different contract documents render differently and that “a change in any field changes the projection” at [docs/task-contracts.md:305](/Users/josh/particle-workers/recallweave/docs/task-contracts.md:305). The renderer intentionally omits several canonical JSON fields, including connection `score`, `evidence`, and `evidence_class`, and retrieved-context metadata such as `title`, `status`, `domain`, and `verified`.

Therefore, even after fixing the connection delimiter collision, changing one of those document fields still produces identical Markdown.

The purported injectivity suite at [tests/test_contract_projection.py:119](/Users/josh/particle-workers/recallweave/tests/test_contract_projection.py:119) covers three selected collision shapes, ordering, multiplicity, and a subset of optional fields. Its populated completeness fixture contains no connections, and its expected labels at [tests/test_contract_projection.py:287](/Users/josh/particle-workers/recallweave/tests/test_contract_projection.py:287) omit the entire connection section. It does not prove the stated document-wide property.

Either define injectivity explicitly over a named projection schema, or render every canonical field with unambiguous boundaries.

## 2. Missing adversarial tests

- Two connections differing only in newline placement between `source` and `target`, using the reproduced collision above.
- Separate labeled blocks for connection `source`, `target`, `kind`, and `verified`, with exact value-to-label attribution.
- Connection absent-versus-empty cases for every optional projected field.
- Two canonical documents differing only in connection `score`, `evidence`, or `evidence_class`.
- Two canonical documents differing only in retrieved `title`, `status`, `domain`, or `verified`.
- A projection test that enumerates every field the Markdown projection claims to preserve, including connections.
- End-to-end indexing/export with newline-bearing vault filenames on both connection endpoints.
- Boundary tests whose field values contain renderer-like lines such as `target:`, `kind:`, and `verified:`.

## 3. Positive findings

- The supplied gate report records 345 tests passing and `compileall` succeeding.
- Constraint statement, citation, and evidence class now render in distinct labeled fences.
- Prior-decision fields use the same separation.
- Retrieved citation, passage, and evidence class are separated.
- Acceptance-criterion ID and statement are separated.
- `None` and the empty string are distinguishable for the optional fields covered by the new tests.
- Fence length remains greater than the longest backtick run in content, and CR/CRLF normalization remains centralized.
- No new document value was found in a Markdown-active position; the remaining defect is semantic boundary ambiguity inside a fence, not fence escape.
- The earlier top-level `schema_version` regression remains fixed.
- The recorded suite supports closure of the earlier destination-protocol, budget, exclusion, and Markdown-inertness regressions.

## 4. Ranked recommended fixes

1. Render every connection field in its own `_field(...)` block, at minimum `source`, `target`, `kind`, `verified`, and `evidence_class`.
2. Decide and document the precise injectivity domain:
   - render all canonical fields if injectivity applies to complete documents; or
   - define a versioned Markdown projection schema and limit the promise to its fields.
3. Add connection cases to both injectivity and projection-completeness tests.
4. Add an end-to-end newline-bearing filename regression.
5. Remove the duplicated injectivity prose and make the `None recorded.` spelling consistent.

## 5. Push safety

The tree is **not safe to push**. The unresolved connection-boundary collision is a High evidence-integrity defect, and the release documentation currently asserts an invariant the implementation does not satisfy.