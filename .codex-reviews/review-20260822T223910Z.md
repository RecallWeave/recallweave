VERDICT: FAIL

## 1. Findings

### High — authored-link re-derivation loses the indexer’s parser state

[src/recallweave/contract.py](/Users/josh/particle-workers/recallweave/src/recallweave/contract.py:416) calls `_links([physical_line], 0)` on an isolated line. But [src/recallweave/parser.py](/Users/josh/particle-workers/recallweave/src/recallweave/parser.py:368) parses the complete input while maintaining fenced-code state across lines.

Consequently, link-looking text inside an already-open code fence is ignored by the indexer but accepted by the exporter as proof of an authored, verified relationship. This contradicts the re-derivation claim in [docs/task-contracts.md](/Users/josh/particle-workers/recallweave/docs/task-contracts.md:562) and the verified-edge contract in [README.md](/Users/josh/particle-workers/recallweave/README.md:18).

In-memory reproduction:

```text
indexed section:
```
[[Target]]
```

persisted edge evidence:
{"line": 2, "source_text": "[[Target]]", "target_text": "Target"}
```

Output:

```text
exporter isolated-line re-derivation: True
indexer whole-input parse: []
```

Thus a hand-inserted edge can still export as `verified: true` even though the indexer would never create it. This is an evidence-integrity violation and keeps the cycle-22 High open at the parser-context layer.

The `note_names` de-duplication itself is correct: distinct `(normalized_name, note_id)` pairs reproduce the indexer’s unique-note lookup, while duplicate title/stem rows for one note do not create false ambiguity. The resolver and exact-path reconstruction also match `index.py`.

## 2. Missing adversarial tests

- Build a note containing `[[Target]]` inside a fenced code block, verify the real index creates no authored edge, insert the corresponding persisted edge, and require contract export to fail.
- Repeat with a Markdown link inside a fence.
- Cover fences opened before the selected section or spanning section boundaries; parsing only the covering section may still lose state.
- Mutation test replacing full-context parsing with isolated-line parsing; it must fail.
- Compare exporter authentication against the actual set of links produced by a real index for stateful parser constructs, rather than merely checking expected fields.

## 3. Positive findings

- Harness results are clean: 408 tests passed normally and under `-W error::ResourceWarning`; `compileall` passed.
- No source changes are present. The only working-tree change is passive `.beads/issues.jsonl` export churn.
- Cycle-22’s no-link, wrong-target, wrong-kind, wrong-line, ambiguous-name, and path-resolution cases are fixed.
- Candidate scores are now accurately documented as persisted and range-checked, not recomputed.
- Name ambiguity is evaluated by distinct note IDs, including both possible ambiguous endpoints.
- Earlier security, privacy, budget, projection, citation-attribution, evidence-shape, candidate-evidence, edge-envelope, and Markdown-inertness findings remain closed by the current code and targeted tests.
- Failure diagnostics for malformed or unauthenticated connections remain content-free.
- Runtime dependency posture remains unchanged.

## 4. Ranked recommended fixes

1. Re-derive links with the same parser context used during indexing. Reconstruct indexed lines through the claimed coordinate and preserve fence state; parsing only the claimed line or only its immediate section is insufficient.
2. Add real-index differential tests proving that an edge authenticates only if the indexer’s full-input parser produced the corresponding link.
3. Update the re-derivation documentation only if the implementation cannot reproduce full parser context from the stored snapshot.

## 5. Push safety

The checkpoint tree may be pushed for durability under the project’s checkpoint policy, but it is **not safe to merge into protected `main`**, promote, release, or deploy. The unresolved High evidence-integrity defect blocks promotion.