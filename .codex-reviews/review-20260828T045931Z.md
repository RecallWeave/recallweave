VERDICT: PASS WITH FIXES

## 1. Findings

### Medium — Domain guarantee still disagrees with the documented contract

[docs/cold-trails.md:159](/Users/josh/particle-workers/recallweave/docs/cold-trails.md:159) requires at least three covered domains whenever the graph contains three or more.

The remediation instead activates the guarantee only when the generated trail pool spans three domains at [viewer/app/cold-trails.ts:936](/Users/josh/particle-workers/recallweave/viewer/app/cold-trails.ts:936). A three-domain graph whose generated pool covers only one or two domains can therefore return `status: "ok"` without satisfying the documented invariant.

Conversely, `poolDomains` includes trails without applying `isTrailEligible`, whose feedback, hub, duplicate-node, type-cap, and domain-cap checks are defined at [viewer/app/cold-trails.ts:647](/Users/josh/particle-workers/recallweave/viewer/app/cold-trails.ts:647). Consequently, an ineligible third-domain trail can trigger refusal even though only one or two domains are actually selectable.

The added regression test at [viewer/tests/cold-trails.test.mjs:690](/Users/josh/particle-workers/recallweave/viewer/tests/cold-trails.test.mjs:690) covers the successful three-domain case, but neither boundary above. This is a contract/oracle defect rather than a demonstrated security or privacy failure.

### Low — Review context and working-tree state are stale

The brief identifies Cycle 26d and a 504-test target. The checkout is post-PR #5 `foundry/steward` at `12f6cf2`, and the supplied harness reports 506 Python tests.

The tree is also dirty with four untracked files:

```text
.codex-reviews/review-20260828T025857Z.md
.codex-reviews/review-20260828T030255Z.md
.codex-reviews/review-20260828T030535Z.md
uv.lock
```

This violates the repository’s clean-tree checkpoint-push condition.

## 2. Missing adversarial tests

- A graph with three declared domains but trails generated from only two; assert the intended success/refusal behavior.
- A raw pool spanning three domains where every trail in the third domain is ineligible because of feedback, hub exclusion, node reuse, or domain-touch limits.
- A mutation test replacing eligible-domain computation with raw-pool-domain computation.
- A final aggregate oracle for every successful tour: structural first, type cap, domain-touch cap, unique nodes, hub exclusion, eligible-domain coverage, and Reinforced ordering.
- A repeated-tour test proving prior feedback does not cause an erroneous three-domain refusal.

## 3. Positive findings

- The supplied harness reports 506 Python tests passing with one skip, both normally and with `ResourceWarning` promoted to an error.
- `compileall` passed.
- The viewer production build and all 39 reported JavaScript tests passed.
- `git diff --check 4bd4bcf..HEAD` reports no whitespace errors.
- The cycle-26d scanner coverage remains present for defaults/decorators, match subjects and guards, handle-write receivers, loop `else`, and independent `AugAssign`.
- Candidate admission requires bilateral citations matching their respective endpoint paths.
- Candidate evidence remains visibly classified as non-factual.
- Saved Markdown applies scalar escaping to untrusted titles and values.
- Privacy metadata is reconciled against normalized displayed content.
- No Critical or High security, privacy, evidence-integrity, compatibility, or reproducibility defect was found.

## 4. Ranked recommended fixes

1. Define the domain invariant precisely: graph domains, generated-pool domains, or currently eligible domains.
2. Compute and validate against that same domain set after eligibility constraints are applied.
3. Add both boundary tests described above plus a final aggregate invariant oracle.
4. Refresh the cycle metadata and expected test count.
5. Remove or deliberately track the four untracked files before checkpointing.

## 5. Push safety

The tree is **not safe to push**: the domain-selection contract remains inconsistent and the working tree is not clean.