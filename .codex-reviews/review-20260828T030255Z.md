VERDICT: FAIL

## 1. Findings

### High — Candidate trails require only one citation, violating bilateral evidence integrity

`candidateHasValidCitation()` accepts a candidate when **any one** of the source, target, or legacy flat citations is valid-looking at [viewer/app/cold-trails.ts:133](/Users/josh/particle-workers/recallweave/viewer/app/cold-trails.ts:133). `scoreCandidateTrail()` then admits it at [viewer/app/cold-trails.ts:317](/Users/josh/particle-workers/recallweave/viewer/app/cold-trails.ts:317).

This permits an imported candidate with a source citation but no target citation to appear as qualified evidence. That contradicts the architecture’s requirement that every discovery candidate carry cited source and target passages and blurs a one-sided signal into a bilateral relationship.

The normalization layer does not close this boundary: it independently preserves possibly empty source and target evidence at [viewer/app/graph-data.ts:312](/Users/josh/particle-workers/recallweave/viewer/app/graph-data.ts:312). Therefore a user-supplied graph can reach this path.

### High — The trail UI still suppresses target provenance

The card renders both endpoint passages, but only renders and copies `source_evidence.citation` at [viewer/app/components/ColdTrailsTour.tsx:331](/Users/josh/particle-workers/recallweave/viewer/app/components/ColdTrailsTour.tsx:331). The target passage is displayed without its citation or even a source/target attribution.

Failure scenario: a candidate contains distinct source and target passages. The user sees two adjacent passages but receives provenance for only the first, making the second passage unattributed and preventing verification. This violates the documented “bilateral cited passages” card contract at [docs/cold-trails.md:174](/Users/josh/particle-workers/recallweave/docs/cold-trails.md:174).

The Markdown exporter now emits every available citation, but because admission only requires one, an exported “citation-rich” candidate can still contain only one endpoint citation.

### Medium — “Open source” behavior does not match the shipped contract

The documentation promises an encoded `obsidian://open` link when a reviewed vault name is present at [docs/cold-trails.md:194](/Users/josh/particle-workers/recallweave/docs/cold-trails.md:194). The implementation ignores `vault_label_claim`, derives a path, and always calls the copy-path callback at [viewer/app/components/ColdTrailsTour.tsx:55](/Users/josh/particle-workers/recallweave/viewer/app/components/ColdTrailsTour.tsx:55) and [viewer/app/components/ColdTrailsTour.tsx:193](/Users/josh/particle-workers/recallweave/viewer/app/components/ColdTrailsTour.tsx:193).

Either the implementation or the shipped documentation must change. As written, the control’s name and documented behavior promise more than it performs.

### Low — Checkpoint review marker is stale

[CHECKPOINT_NOT_APPROVED.md:9](/Users/josh/particle-workers/recallweave/CHECKPOINT_NOT_APPROVED.md:9) still names cycle 26c as the latest verdict, despite HEAD containing the later Cold Trails remediation and this review cycle.

### Low — Working tree is not push-clean

`git status --porcelain` reports:

```text
?? .codex-reviews/review-20260828T025857Z.md
?? uv.lock
```

Repository policy requires a clean integration tree before checkpoint push.

## 2. Missing adversarial tests

- A candidate with only a source citation must be refused.
- A candidate with only a target citation must be refused.
- A legacy flat citation without bilateral nested citations must not satisfy the v2 evidence boundary.
- Source and target citations must correspond to their respective endpoint paths.
- The rendered card must visibly associate each passage with its endpoint citation.
- Saved Markdown must refuse or explicitly mark an incomplete candidate rather than exporting one-sided provenance.
- “Open source” with and without a validated vault label, including URI encoding of spaces and reserved characters.
- A test asserting that the checkpoint marker names the newest committed review.

## 3. Positive findings

- The supplied harness ran 505 Python tests successfully with one skip.
- The ResourceWarning-strict Python run also passed.
- `compileall` succeeded.
- The harness now includes the viewer build and JavaScript tests: all 31 reported viewer tests passed.
- Cycle-26d’s scanner remediations cover defaults/decorators, match subjects and guards, bound file-handle writes, and independent loop-else/AugAssign behavior.
- Structural Bridge and Island trails are no longer mislabeled as authored links.
- Markdown export now neutralizes tested title/heading injection and emits available citations.
- Candidate ordering remains deterministic in the tested fixtures.
- No new graph upload, telemetry, vault-write, or direct filesystem-navigation route was found.
- `git diff --check 374be23..HEAD` reports no whitespace errors.

## 4. Ranked recommended fixes

1. Require valid, endpoint-matching source **and** target citations before admitting a candidate trail.
2. Render both endpoint passages with explicit source/target labels and their respective citations.
3. Make saved exports reject incomplete candidate provenance.
4. Implement the documented reviewed-vault `obsidian://open` behavior or revise the contract and control label to say it copies a path.
5. Add the missing one-sided and endpoint-mismatch adversarial tests.
6. Refresh the checkpoint marker and resolve the untracked files before pushing.

## 5. Push safety

The tree is **not safe to push**. It has unresolved High evidence-integrity defects and is not clean.