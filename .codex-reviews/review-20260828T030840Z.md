VERDICT: PASS WITH FIXES

## 1. Findings

### Medium — Genuine exporter output is falsely reported as conflicting

The producer counts `changed` and `unchanged` only for nodes present in both exports at [src/recallweave/viewer.py:178](/Users/josh/particle-workers/recallweave/src/recallweave/viewer.py:178). The consumer instead requires `changed + unchanged` to equal every currently hashed node at [viewer/app/graph-data.ts:225](/Users/josh/particle-workers/recallweave/viewer/app/graph-data.ts:225).

For a first export containing `N` hashed nodes, the producer emits:

```text
changed=0, unchanged=0, nodes_added=N
```

The consumer evaluates `0 != N` and sets `claim_conflict: true`. Exports containing subsequently added nodes fail similarly. Atlas therefore labels valid provenance generated by RecallWeave itself as conflicting.

The tests conceal the mismatch:

- [tests/test_viewer.py:86](/Users/josh/particle-workers/recallweave/tests/test_viewer.py:86) checks key presence and only a lower bound on unchanged nodes; it never passes the produced document through `normalizeGraph`.
- [viewer/tests/graph-data.test.mjs:451](/Users/josh/particle-workers/recallweave/viewer/tests/graph-data.test.mjs:451) hand-constructs impossible first-export counters: two added nodes are also counted as changed/unchanged.

### Medium — Cold Trails does not implement its documented exact scoring and selection contract

The documentation promises exact behavior at [docs/cold-trails.md:99](/Users/josh/particle-workers/recallweave/docs/cold-trails.md:99) and [docs/cold-trails.md:152](/Users/josh/particle-workers/recallweave/docs/cold-trails.md:152), but the runtime diverges in several material ways:

- [viewer/app/cold-trails.ts:296](/Users/josh/particle-workers/recallweave/viewer/app/cold-trails.ts:296) awards the `0.3` passage bonus when either endpoint has passage text; the contract requires both endpoints.
- The documented `-0.15` redundant-surprise-term penalty is never calculated.
- Selection is sorted once at [viewer/app/cold-trails.ts:593](/Users/josh/particle-workers/recallweave/viewer/app/cold-trails.ts:593), rather than rescored after each choice.
- [viewer/app/cold-trails.ts:627](/Users/josh/particle-workers/recallweave/viewer/app/cold-trails.ts:627) tracks domains as a set, not touch counts, so it does not implement “at most two touching one domain.”
- There is no 95th-percentile hub exclusion.
- Candidate classification does not explicitly enforce the promised “at least two surprise conditions.”

These discrepancies make the displayed “exact score breakdown” and deterministic selection contract unreliable. Existing tests mostly establish that trail types appear, not that scores and constraints are correct.

### Medium — The v2 exporter accepts path-like vault labels that its schema forbids

[docs/json-output.md:338](/Users/josh/particle-workers/recallweave/docs/json-output.md:338) says `vault_name` is never a filesystem path. The CLI accepts arbitrary text, and [src/recallweave/viewer.py:355](/Users/josh/particle-workers/recallweave/src/recallweave/viewer.py:355) serializes it without validation. Thus `--vault-name /Users/alice/PrivateVault` embeds a local absolute path in the graph.

The Atlas importer later rejects path-like labels, but downstream sanitization is not an export safety boundary. The Python exporter needs the same validation.

### Low — Documentation simultaneously declares v1 and v2 as the emitted schema

[docs/json-output.md:127](/Users/josh/particle-workers/recallweave/docs/json-output.md:127) says both graph and receipt use `recallweave.viewer.v1`, while [docs/json-output.md:302](/Users/josh/particle-workers/recallweave/docs/json-output.md:302) says both use v2. Runtime emits v2. This is an internally contradictory public output contract.

[README.md:193](/Users/josh/particle-workers/recallweave/README.md:193) also still describes the now-implemented Cold Trails feature as design-only and planned.

### Low — Review context and working tree are stale/dirty

The supplied cycle describes PR #5 turn-1 remediation, but `HEAD` is nine commits beyond merged PR #5 commit `4bd4bcf`, containing viewer.v2 and Cold Trails implementation.

The working tree contains four untracked files:

```text
.codex-reviews/review-20260828T025857Z.md
.codex-reviews/review-20260828T030255Z.md
.codex-reviews/review-20260828T030535Z.md
uv.lock
```

That violates the integration branch’s clean-tree push gate.

## 2. Missing adversarial tests

- Export a first viewer.v2 graph, normalize that exact output, and assert `claim_conflict === false`.
- Repeat after adding, removing, changing, and retaining nodes; assert every counter and conflict decision exactly.
- Assert one-sided passage text does not receive the bilateral `0.3` evidence bonus.
- Assert redundant surprise terms apply `-0.15` during greedy rescoring.
- Exercise three trails touching the same domain and require the third to be excluded.
- Exercise a node above the 95th degree percentile for every non-Bridge type.
- Construct Reinforced candidates satisfying only one documented surprise condition and require refusal.
- Reject absolute, relative, Windows-drive, UNC, traversal, URI-like, and control-character `--vault-name` values in the Python exporter.
- Assert documentation examples and emitted schema constants agree.
- Mutation tests should fail when bilateral passage scoring, rescoring, domain counters, hub exclusion, or vault-label validation is removed.

## 3. Positive findings

- The harness reports 505 Python tests passing with one skip, both normally and with `ResourceWarning` promoted to an error.
- `compileall` succeeded.
- The viewer production build and all 33 reported JavaScript tests passed.
- Cycle-26d’s scanner remediations for defaults/decorators, match subjects and guards, handle writes, loop `else`, and independent `AugAssign` handling remain present and covered.
- Cold Trails candidate admission requires valid citations for both endpoints and checks each citation against its corresponding node.
- Candidate evidence remains explicitly labeled as non-factual.
- Markdown session exports neutralize tested heading/list/backtick injection.
- Atlas recomputes privacy profiles from normalized content instead of trusting declared flags.
- Duplicate/dangling graph records and unsafe controls/default-ignorables are handled conservatively.
- Direct vault navigation is not exposed; relative paths are copied instead.
- `git diff --check 4bd4bcf..HEAD` reports no whitespace errors.

## 4. Ranked recommended fixes

1. Reconcile export-history arithmetic and add an end-to-end producer-to-consumer test.
2. Implement the documented Cold Trails scoring, qualification, rescoring, domain-count, and hub constraints—or revise the contract before calling the breakdown exact.
3. Validate `vault_name` in the Python export boundary and reject path-like input.
4. Remove the contradictory v1 section and update the README’s Cold Trails status.
5. Resolve the four untracked files and refresh the cycle/review metadata.

## 5. Push safety

The tree is **not safe to push**. No Critical or High defect was found, but Medium correctness and export-boundary issues remain, and the integration tree is not clean.