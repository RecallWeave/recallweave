VERDICT: PASS WITH FIXES

## 1. Findings

### Medium — truncated connection evidence may omit its truncation flag

A persisted connection-evidence side is considered well formed when it contains only `citation` and `passage`; `truncated` remains optional:

- [contract.py](/Users/josh/particle-workers/recallweave/src/recallweave/contract.py:121) requires only the substantive `passage` leaf and a citation.
- [contract.py](/Users/josh/particle-workers/recallweave/src/recallweave/contract.py:215) compares `truncated` only when that leaf is present.
- [docs/task-contracts.md](/Users/josh/particle-workers/recallweave/docs/task-contracts.md:177) explicitly documents all four side leaves as optional.
- [ARCHITECTURE.md](/Users/josh/particle-workers/recallweave/ARCHITECTURE.md:97) promises that when a returned passage is shortened, its `truncated` field is `true`.

Concrete scenario: take an authentic long indexed passage, preserve its exact bounded text and citation, but delete `"truncated": true` from the persisted side. The attribution check succeeds because the passage and coordinates match and the absent flag is never compared. The JSON artifact consequently carries a shortened passage without the field that declares it shortened.

A read-only predicate reproduction confirms the accepted shape:

```text
side = {
    "citation": "Alpha.md:1-2",
    "passage": "x" * 499 + "…"
}
connection_evidence_is_well_formed(...) => True
```

This is the next level below cycle 18’s equality fix: every supplied value is checked, but the complete index-produced side shape is not required. It does not permit fabricated passage text, so I classify it Medium rather than High, but it contradicts the citation contract and blocks promotion under this cycle’s explicit “anything at any severity” rule.

The analogous omission of `heading` also produces a side shape the current indexer never emits. That omission is less harmful because it does not make a false truncation claim, but the fix should require the complete index-produced leaf set rather than special-case only `truncated`.

No unresolved Critical or High finding remains.

## 2. Missing adversarial tests

- Generate a genuinely truncated indexed side, delete only `truncated`, and require the full export to fail closed.
- Generate a non-truncated indexed side, delete only `truncated`, and pin whether complete indexer-shape equality is required consistently.
- Delete only `heading` from an otherwise authentic side and require rejection if the contract is that persisted sides reproduce `cited_passage()`.
- Test both source and target sides with missing authenticated leaves; current mutation tests principally exercise altered values.
- Assert that every connection side emitted by the public builder has exactly `citation`, `heading`, `passage`, and `truncated`, not merely a permitted subset.

## 3. Positive findings

- The supplied harness results are clean: 395 tests passed normally, 395 passed with `ResourceWarning` promoted to an error, and `compileall` passed.
- Cycle 18’s High is otherwise fixed: fabricated passages, headings, truncation values, and one-character passage changes behind valid coordinates fail attribution.
- Expected passage construction matches the indexer’s actual order of truncation, ellipsis insertion, sanitization, and final bounding.
- Genuine long passages retain their ellipsis and authenticate successfully.
- Attribution is correctly scoped to the indexed snapshot. Editing the vault after indexing does not silently change the contract’s evidence boundary, and the documentation now identifies `provenance.index.indexed_at` as its age.
- Citation inventory is exact for admitted connections, including deduplication, source-before-target order, and omission of budget-excluded connections.
- Diagnostics remain content-free: they identify the database edge ID without echoing endpoint paths, citations, headings, or passages.
- Excluded endpoints are suppressed before evidence validation.
- The Markdown renderer remains inert and preserves one field per fenced block.
- The working tree is clean and matches `origin/foundry/task-contracts`.

Across the eighteen-cycle history, the prior Critical/High classes remain closed: Markdown injection through block, inline, multiline, CR, and handling-text routes; field-boundary collisions; schema-identity regression; hard-budget overflow; falsy/null compatibility regression; structural-absence forgery; projected-field injectivity failures; false connection-evidence omission claims; malformed emitted evidence; excluded-content leakage; endpoint-path disclosure; missing or fabricated connection citations; and fabricated passages behind valid citations.

## 4. Ranked recommended fixes

1. Require every present connection-evidence side to contain exactly `citation`, `heading`, `passage`, and `truncated`, matching the shape produced by `index.py:cited_passage()`.
2. Compare all four leaves unconditionally after that shape check.
3. Add the missing-leaf adversarial tests above, including both truncated and non-truncated passages and both evidence sides.
4. Keep the existing indexed-snapshot documentation; no vault-time verification should be implied.

## 5. Push safety

The clean tree is safe to push only as a non-approved checkpoint.

It is **not safe to merge into protected `main`**, promote, release, or deploy. The Medium truncation-integrity defect must be fixed and reviewed before promotion.