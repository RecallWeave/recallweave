VERDICT: FAIL

## 1. Findings

### High — malformed-evidence errors disclose vault note paths

[src/recallweave/contract.py](/Users/josh/particle-workers/recallweave/src/recallweave/contract.py:460) constructs the failure message using `source_path` and `target_path`. [src/recallweave/cli.py](/Users/josh/particle-workers/recallweave/src/recallweave/cli.py:280) then serializes that message verbatim to stderr.

Concrete scenario: a malformed edge between `People/Medical Diagnosis.md` and `Legal/Acquisition Target.md` causes both vault-relative paths to appear in structured CLI output, despite no contract artifact being produced. Those names are vault-derived metadata and can disclose people, projects, health information, or organizational structure. [PRIVACY.md](/Users/josh/particle-workers/recallweave/PRIVACY.md:89) explicitly recognizes relative paths as sensitive bundle content, but does not warn that failure receipts can contain them.

The new CLI test at [tests/test_contract_cli.py](/Users/josh/particle-workers/recallweave/tests/test_contract_cli.py:146) verifies the error route but does not assert that sensitive endpoint names are absent. The remediation intentionally introduced this disclosure while fixing the validator bypass.

Fix: identify the offending edge using a non-content-bearing database edge ID or generic remediation message. If detailed paths are needed for local diagnosis, require an explicit diagnostic mode and document its disclosure implications.

### Medium — the claimed exhaustive partition is not tested across public builder shapes

The canonical-leaf helper treats an empty collection as a leaf such as `connections[]` or `acceptance_criteria[]` at [tests/test_contract_document.py](/Users/josh/particle-workers/recallweave/tests/test_contract_document.py:41). However, the partition test exercises only one populated corpus at [tests/test_contract_document.py](/Users/josh/particle-workers/recallweave/tests/test_contract_document.py:294).

Publicly constructible documents with no query, criteria, constraints, decisions, citations, connections, or exclusion entries therefore have leaves such as:

- `retrieved_context[]`
- `connections[]`
- `constraints[]`
- `prior_decisions[]`
- `acceptance_criteria[]`
- `provenance.citations[]`

These names are in neither inventory. Running the existing partition logic against such a public builder result would contradict the exhaustive claim in [docs/task-contracts.md](/Users/josh/particle-workers/recallweave/docs/task-contracts.md:374).

This is currently a proof and documentation defect rather than a demonstrated renderer leak.

### Medium — list probes do not prove omitted collections are unread

At [tests/test_contract_projection.py](/Users/josh/particle-workers/recallweave/tests/test_contract_projection.py:870), every list probe uses two one-element, non-empty lists. A renderer that reads only `len(value)`, `bool(value)`, or emptiness would produce identical output for both probes, so the invariance test at [tests/test_contract_projection.py](/Users/josh/particle-workers/recallweave/tests/test_contract_projection.py:1267) would pass even though the omitted field influenced Markdown.

This makes the documentation’s statement that each omitted field is “proved unreadable” too strong. Static inspection indicates the current renderer does not read `retrieved_context[].matched_terms`, so this is a test-integrity defect rather than an active disclosure.

## 2. Missing adversarial tests

- A malformed edge whose endpoint paths contain sensitive sentinels; assert neither sentinel appears in stdout, stderr, or an artifact.
- The partition check over a minimal public builder result containing empty collections.
- Partition checks over independently varied builder shapes: no query, zero retrieval results, no connections, no criteria, and empty exclusions.
- For every omitted list, compare empty, one-element, and multi-element values—not merely different one-element values.
- An error-route disclosure test covering both malformed non-excluded endpoints and an excluded endpoint, ensuring excluded paths never reach diagnostics.

## 3. Positive findings

- The supplied harness transcript reports 379 tests passing and `compileall` succeeding.
- The malformed-evidence validator now runs before the connection budget check.
- One malformed edge aborts the whole export; malformed evidence is neither rendered nor silently suppressed.
- The normal export route funnels document construction through `build_contract_document`.
- Static inspection found no document-derived value escaping fences through the structural-absence implementation. A marker-valued field remains fenced, while genuine absence is renderer-authored chrome.
- The current renderer does not appear to consume the documented omitted fields.
- The working tree is clean, and `HEAD` matches `origin/foundry/task-contracts`.

## 4. Ranked recommended fixes

1. Remove vault paths from the malformed-evidence exception and add sentinel-based CLI leakage coverage.
2. Define whether empty collection containers count as canonical leaves, then make the inventory, helper, tests, and documentation consistent across all public builder shapes.
3. Strengthen omitted-list probes with different cardinalities and empty/non-empty cases.
4. Document the sensitivity of all structured error receipts if any operational identifiers remain exposed.

## 5. Push safety

The tree is **not safe to push** under this review gate because the malformed-evidence error route has an unresolved High privacy disclosure.