VERDICT: FAIL

## 1. Findings

### High — Injectivity still fails over the documented projected field set

The contract promises that documents differing in any projected field never render identically ([docs/task-contracts.md:342](/Users/josh/particle-workers/recallweave/docs/task-contracts.md:342)), and specifically promises absence is distinguishable from emptiness ([docs/task-contracts.md:361](/Users/josh/particle-workers/recallweave/docs/task-contracts.md:361)).

However, several projected fields are conditionally omitted when falsey:

- `task.id` and `task.objective`: [contract_markdown.py:146](/Users/josh/particle-workers/recallweave/src/recallweave/contract_markdown.py:146)
- `handling.statement` and `handling.scope`: [contract_markdown.py:128](/Users/josh/particle-workers/recallweave/src/recallweave/contract_markdown.py:128)
- `provenance.generated_at` and index fields: [contract_markdown.py:290](/Users/josh/particle-workers/recallweave/src/recallweave/contract_markdown.py:290)

Thus `None` and `""` render byte-identically. The renderer also normalizes CRLF/CR to LF at [contract_markdown.py:87](/Users/josh/particle-workers/recallweave/src/recallweave/contract_markdown.py:87), so distinct projected string values can render identically.

Reproduction on the current tree:

```text
task.id None_vs_empty True
objective LF_vs_CRLF True
handling.statement None_vs_empty True
```

The new test only changes populated values to `"CHANGED"` ([test_contract_projection.py:476](/Users/josh/particle-workers/recallweave/tests/test_contract_projection.py:476)). That establishes field influence at one point, not injectivity. Because reproducibility/injectivity is explicitly contractual, this requires `FAIL`.

### High — The human connection artifact omits the evidence needed to evaluate a connection

The canonical connection carries `score`, `evidence`, and `evidence_class` ([task-contracts.md:159](/Users/josh/particle-workers/recallweave/docs/task-contracts.md:159)), but Markdown renders only `source`, `target`, `kind`, and `verified` ([contract_markdown.py:226](/Users/josh/particle-workers/recallweave/src/recallweave/contract_markdown.py:226)).

The new specification says readers do not need the omitted evidence detail ([task-contracts.md:330](/Users/josh/particle-workers/recallweave/docs/task-contracts.md:330)). That is not an honest human task artifact for this repository’s evidence model:

- The architecture requires connections to retain `authored_link` versus `discovery_candidate` evidence classes ([ARCHITECTURE.md:34](/Users/josh/particle-workers/recallweave/ARCHITECTURE.md:34)).
- Discovery candidates are supposed to carry cited bilateral evidence ([ARCHITECTURE.md:77](/Users/josh/particle-workers/recallweave/ARCHITECTURE.md:77)).
- The character budget explicitly pays for connection evidence ([task-contracts.md:201](/Users/josh/particle-workers/recallweave/docs/task-contracts.md:201)).

A receiving human or agent sees that two notes are connected but cannot inspect why, its score, or the cited passages already admitted under the budget. This reduces evidence to an unexplained assertion and violates the repository’s evidence-integrity boundary. Calling the omission intentional does not cure the defect.

### Medium — Privacy disclosure is absent from the human artifact

The canonical `disclosure` object describes content actually present ([task-contracts.md:213](/Users/josh/particle-workers/recallweave/docs/task-contracts.md:213)), but the entire object is omitted from both the projected-field set and renderer.

This matters for a portable, human-reviewed artifact: the Markdown does not carry `profile`, `includes_passage_text`, `includes_paths_titles_tags`, `includes_candidate_edges`, or `includes_operator_statements`. The user must inspect canonical JSON to recover the privacy claims that explain what the artifact contains.

## 2. Missing adversarial tests

- For every nullable/optional projected scalar, compare `None`, missing key, and `""`; assert the documented distinctions.
- Compare `"\n"`, `"\r\n"`, and `"\r"` inside every projected string and either prove injectivity or explicitly define canonical line-ending equivalence.
- Property-based pairwise injectivity tests over projected values, rather than one populated baseline and one `"CHANGED"` mutation.
- Assert that Markdown connections expose `evidence_class`, score, and bounded evidence with correct value-to-label attribution.
- Assert Markdown carries actual-content disclosure flags and that they agree with the rendered content.
- Pin the projected set against the canonical document schema, with an explicit reviewed disposition for every omitted field.

## 3. Positive findings

- The cycle-9 connection collision is fixed. The supplied source/target collision pair now renders differently:

```text
cycle9_connection_collision False
```

- Connection `source`, `target`, `kind`, and `verified` now have distinct labels and fences.
- The adaptive fence length continues to keep embedded backticks inert.
- The documentation and `PROJECTED_FIELDS` list currently agree mechanically.
- The harness report records all 351 tests passing and `compileall` succeeding. Per the invocation instructions, I did not rerun the suite.
- The working tree was not modified by this review.

## 4. Ranked recommended fixes

1. Make the injectivity claim true: render every projected field regardless of falsey value, distinguish missing/`None`/empty where promised, and resolve line-ending equivalence explicitly.
2. Project connection `evidence_class`, `score`, and evidence into separately labelled inert blocks.
3. Project the disclosure object, or remove claims that Markdown is an adequate human-review artifact.
4. Replace the single-baseline mutation test with boundary and pairwise/property-based injectivity coverage.
5. Add a schema-to-projection disposition test so undocumented omissions cannot be introduced silently.

## 5. Push safety

The tree is not safe to push. The documented injectivity/reproducibility contract is false, and the Markdown connection artifact omits material evidence needed to preserve evidence-class integrity.