VERDICT: FAIL

## 1. Findings

### High — Python exporter truncates fractional timestamps and can misclassify Parallel invention trails

[src/recallweave/viewer.py:40](/Users/josh/particle-workers/recallweave/src/recallweave/viewer.py:40) parses timestamps correctly, but [line 61](/Users/josh/particle-workers/recallweave/src/recallweave/viewer.py:61) serializes them with `strftime("%Y-%m-%dT%H:%M:%SZ")`, discarding all fractional seconds.

Reproduction on the current tree:

```text
2026-01-01T00:00:00.001Z => 2026-01-01T00:00:00Z
2026-01-01T00:00:00.999999+00:00 => 2026-01-01T00:00:00Z
2026-01-01T01:00:00.123+01:00 => 2026-01-01T00:00:00Z
```

This defeats the cycle’s claimed fractional-timestamp remediation. In particular:

- `2026-06-01T00:00:00.000Z`
- `2026-06-15T00:00:00.001Z`

are more than 14 days apart and must not qualify as Parallel invention. After Python export, both become second-resolution values exactly 14 days apart, so [viewer/app/cold-trails.ts:561](/Users/josh/particle-workers/recallweave/viewer/app/cold-trails.ts:561) can incorrectly classify the candidate as `parallel_invention`.

The new JS test at [viewer/tests/cold-trails.test.mjs:1702](/Users/josh/particle-workers/recallweave/viewer/tests/cold-trails.test.mjs:1702) validates only a hand-built JSON document passed through `normalizeGraph()`. It never crosses the Python producer boundary where precision is lost.

This also contradicts the v2 contract at [docs/json-output.md:332](/Users/josh/particle-workers/recallweave/docs/json-output.md:332), which promises the creation timestamp “as recorded by the index.”

No Critical, Medium, or Low findings beyond this High defect.

## 2. Missing adversarial tests

- Python `_nullable_timestamp()` test asserting that nonzero fractional seconds survive UTC normalization.
- Python test covering a fractional timestamp with a nonzero timezone offset.
- End-to-end index → `build_viewer_document()` → `normalizeGraph()` → `buildColdTrails()` test for the exact `14 days + 1 ms` exclusion.
- Mutation test replacing fraction-preserving Python serialization with second-only `strftime`; the suite should fail under that mutation.

## 3. Positive findings

- Harness results are green: 508 Python tests with one skip, a ResourceWarning-clean rerun, successful `compileall`, and all 55 viewer tests. See [.codex-reviews/suite-20260828T110850Z.txt](/Users/josh/particle-workers/recallweave/.codex-reviews/suite-20260828T110850Z.txt).
- Graph-level timezone-less `generated_at` values are rejected rather than interpreted in the browser’s local timezone.
- Invalid graph clocks do not fall back to node timestamps for Dormant scoring.
- Tours now require a Bridge or Island opening.
- Conflicted export-history claims are excluded from Drift scoring and displayed facts.
- Candidate citations are matched against normalized node paths rather than ids.
- Show-another excludes every pair from the current tour, and keyboard handling avoids hijacking typing fields and activated controls.
- Base-to-head `git diff --check` passed.
- The tracked working tree is clean; the review did not modify it.

## 4. Ranked recommended fixes

1. Serialize normalized UTC timestamps without discarding nonzero fractional seconds. Preserve the parsed precision, or emit a canonical UTC representation retaining the microsecond component and omit it only when zero.
2. Add the Python and end-to-end boundary tests listed above.
3. Keep the existing browser fractional-boundary test as consumer-side coverage.

## 5. Push safety

The tree is **not safe to push or promote**. The unresolved High correctness and contract-integrity defect requires a `FAIL` verdict.