VERDICT: PASS WITH FIXES

## 1. Findings

### Medium — Cold Trails still violates its published exact scoring and selection contract

The documentation promises greedy rescoring and exact diversification constraints at [docs/cold-trails.md:122](/Users/josh/particle-workers/recallweave/docs/cold-trails.md:122) and [docs/cold-trails.md:152](/Users/josh/particle-workers/recallweave/docs/cold-trails.md:152), but the runtime differs:

- The documented `-0.15` redundant-surprise-term penalty is never calculated in [cold-trails.ts:355](/Users/josh/particle-workers/recallweave/viewer/app/cold-trails.ts:355).
- Trails are scored and sorted once, rather than rescored after every selection, at [cold-trails.ts:591](/Users/josh/particle-workers/recallweave/viewer/app/cold-trails.ts:591).
- Domain usage is represented by a set, not touch counts, so “at most two touching one domain” is not enforced at [cold-trails.ts:624](/Users/josh/particle-workers/recallweave/viewer/app/cold-trails.ts:624).
- No 95th-percentile hub exclusion exists, despite [docs/cold-trails.md:162](/Users/josh/particle-workers/recallweave/docs/cold-trails.md:162).
- The promised sequencing and three-domain coverage are not enforced.
- Bridge and Island score breakdowns contain fixed component values whose weighted formula does not produce their reported totals at [cold-trails.ts:448](/Users/josh/particle-workers/recallweave/viewer/app/cold-trails.ts:448) and [cold-trails.ts:493](/Users/josh/particle-workers/recallweave/viewer/app/cold-trails.ts:493).

Consequently, the UI’s advertised “exact score” can be arithmetically false, and selection does not satisfy the deterministic contract it claims to implement.

### Medium — Export-history conflict detection accepts impossible subsequent-export claims

[graph-data.ts:225](/Users/josh/particle-workers/recallweave/viewer/app/graph-data.ts:225) marks a subsequent history record conflicting only when:

```text
changed + unchanged > hashedNodes
```

It therefore accepts, for example, a graph with two current nodes, a non-null prior digest, `nodes_added=0`, `changed=0`, and `unchanged=0`. Those counters cannot describe the current graph: every non-added node must be classified as changed or unchanged.

The first-export remediation is correct, but subsequent-export arithmetic remains under-validated. Current tests cover first-export shape and an overflow case, not omitted overlap counts.

### Low — Review context and working tree are not push-clean

The supplied cycle describes PR #5 turn-1 remediation, while `HEAD` is the post-PR `foundry/steward` branch containing viewer.v2 and Cold Trails work.

`git status` reports:

```text
M  .beads/issues.jsonl
?? .codex-reviews/review-20260828T025857Z.md
?? .codex-reviews/review-20260828T030255Z.md
?? .codex-reviews/review-20260828T030535Z.md
?? uv.lock
```

Repository policy requires a clean integration tree before pushing.

## 2. Missing adversarial tests

- Assert that every displayed score equals its documented weighted components.
- Assert redundant surprise terms apply the `0.15` penalty during greedy rescoring.
- Construct three otherwise eligible trails touching one domain and require the third to be excluded.
- Require non-Bridge trails touching a node above the 95th degree percentile to be excluded.
- Require the selected tour to cover three domains when feasible.
- Assert the documented structural-first and Reinforced-last sequencing.
- Normalize a subsequent export with missing overlap counters and assert `claim_conflict === true`.
- Feed the exact Python-produced first and replacement exports through `normalizeGraph`, checking all counters end to end.
- Add mutation tests for rescoring, domain touch counts, hub exclusion, and history arithmetic.

## 3. Positive findings

- The supplied harness report records 506 Python tests passing with one skip, both normally and with `ResourceWarning` promoted to an error.
- `compileall` completed successfully.
- The viewer production build and all 34 reported JavaScript tests passed.
- Cycle-26d’s defaults/decorators, match-subject/guard, handle-write, loop-`else`, and independent `AugAssign` scanner remediations remain present with targeted regression tests.
- Path-like `vault_name` values are rejected at the Python export boundary.
- Candidate admission requires bilateral endpoint-matching citations.
- The evidence passage bonus now requires passages on both endpoints.
- Candidate evidence remains explicitly labeled non-factual.
- Saved Markdown neutralizes the tested title and structural-fact injection forms.
- Atlas recomputes privacy classifications from normalized graph content.
- Direct vault navigation remains disabled.
- `git diff --check 4bd4bcf..HEAD` reports no whitespace errors.

## 4. Ranked recommended fixes

1. Make Cold Trails scoring and greedy selection implement the documented formula and constraints exactly; calculate structural totals from the same formula.
2. Validate history with `changed + unchanged === nodes.length - nodes_added`, with appropriate bounds for all counters.
3. Add exact behavioral and mutation tests rather than presence/type assertions.
4. Refresh the review-cycle metadata and resolve all tracked/untracked working-tree changes.

## 5. Push safety

The tree is **not safe to push** under the repository’s clean-tree gate. No Critical, High, privacy, security-boundary, or evidence-integrity defect was found, but the Medium runtime-contract discrepancies remain and the working tree is dirty.